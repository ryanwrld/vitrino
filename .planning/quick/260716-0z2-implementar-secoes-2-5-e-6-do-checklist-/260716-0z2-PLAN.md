---
phase: quick-260716-0z2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/vitrino-wordmark.tsx
  - src/components/accent-color-field.tsx
  - src/components/store-identity-fields.tsx
  - src/components/admin-sidebar.tsx
  - src/app/(admin)/login/page.tsx
  - src/app/(admin)/cadastro/page.tsx
  - src/app/(admin)/esqueci-senha/page.tsx
  - src/app/(admin)/redefinir-senha/page.tsx
  - src/app/(admin)/onboarding/onboarding-wizard.tsx
  - src/app/(admin)/(painel)/configuracoes/settings-form.tsx
  - src/app/(admin)/(painel)/configuracoes/qr-code-panel.tsx
  - src/app/(admin)/(painel)/produtos/product-form.tsx
  - src/app/(admin)/(painel)/produtos/product-list.tsx
  - src/app/(admin)/(painel)/produtos/size-grid.tsx
  - src/app/(admin)/(painel)/dashboard/page.tsx
  - src/app/loja/[slug]/store-hero.tsx
  - src/app/loja/[slug]/pagination-numbered.tsx
  - src/app/loja/[slug]/[produto]/page.tsx
  - src/app/loja/[slug]/[produto]/product-order-panel.tsx
autonomous: true
requirements: [UI-SEC2, UI-SEC5, UI-SEC6]

must_haves:
  truths:
    - "Login, cadastro, esqueci-senha, redefinir-senha e onboarding exibem a marca Vitrino (wordmark em cor de marca)"
    - "Todo input com erro de validação muda a cor da borda para vermelho, além do texto de erro"
    - "'Pedir agora' é visualmente dominante sobre 'Copiar pedido' na página de produto"
    - "Todo hex de cor no className vira um token Tailwind (@theme) — zero `[#hex]` em src/"
    - "Toda a paleta usa exatamente #0D21A1 / #000000 / #E7F2FD / #FF4D4D / #111111 / #6B6B6B (sem cores novas)"
    - "Os 22 itens das seções 2, 5 e 6 do checklist ficam marcados [x]"
  artifacts:
    - src/app/globals.css
    - src/components/vitrino-wordmark.tsx
    - src/components/accent-color-field.tsx
    - src/components/store-identity-fields.tsx
  key_links:
    - "@theme tokens em globals.css consumidos por bg-brand/text-ink/border-surface/text-danger/text-muted em todo src/"
    - "accent-color-field.tsx reusado por onboarding-wizard.tsx e settings-form.tsx (fonte única do seletor de cor)"
---

<objective>
Implementar as seções 2 (Identidade visual / design system), 5 (Hierarquia visual / feedback de formulário) e 6 (Inconsistência de design system) do checklist `.planning/todos/ajustes-finais-ui-mvp.md` — 22 itens de acabamento de UI/UX do MVP Vitrino.

Purpose: Elevar o acabamento visual e a coerência do design system do painel admin, dos fluxos de auth/onboarding e da vitrine pública antes do lançamento, sem alterar comportamento de dados.
Output: Marca presente nos fluxos de confiança, feedback de formulário mais forte, hierarquia visual corrigida, paleta tokenizada como fonte única de verdade, e consistência de botões/status/navegação.

**Escopo:** SOMENTE seções 2, 5, 6. NÃO tocar nas seções 1, 3, 4, 7, 8, 9. Em particular, NÃO corrigir `<html lang="en">`, NÃO trocar `font-family: Arial` por Geist, NÃO mexer no contraste de `#FF4D4D` como texto (são seção 1 e 3, fora deste escopo).

**Paleta oficial (preservar valores exatos, não inventar cores):**
`#0D21A1` (azul de marca) · `#000000` (preto) · `#E7F2FD` (azul claro) · `#FF4D4D` (vermelho) · `#111111` (texto) · `#6B6B6B` (texto secundário).

**Mapa de tokens (usado na Task 6):**
`#0D21A1 → brand` · `#E7F2FD → surface` · `#FF4D4D → danger` · `#111111 → ink` · `#6B6B6B → muted` · `#000000 → black` (utilitário nativo do Tailwind).

**Convenções:** todo texto visível em português; sem dependências novas (Tailwind v4 `@theme` + transições CSS bastam). Ao final de cada task, marcar os checkboxes correspondentes em `.planning/todos/ajustes-finais-ui-mvp.md` (trocar `[ ]` por `[x]`, sem apagar o texto do item).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/ajustes-finais-ui-mvp.md
@.claude/CLAUDE.md
@src/app/globals.css
@src/app/(admin)/login/page.tsx
@src/app/(admin)/onboarding/onboarding-wizard.tsx
@src/app/(admin)/(painel)/configuracoes/settings-form.tsx
@src/app/(admin)/(painel)/produtos/product-form.tsx
@src/app/(admin)/(painel)/produtos/product-list.tsx
@src/app/(admin)/(painel)/produtos/size-grid.tsx
@src/app/(admin)/(painel)/dashboard/page.tsx
@src/components/admin-sidebar.tsx
@src/app/loja/[slug]/store-hero.tsx
@src/app/loja/[slug]/pagination-numbered.tsx
@src/app/loja/[slug]/[produto]/page.tsx
@src/app/loja/[slug]/[produto]/product-order-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feedback de formulário e hierarquia visual (Seção 5, itens 1-4)</name>
  <files>src/app/(admin)/login/page.tsx, src/app/(admin)/cadastro/page.tsx, src/app/(admin)/onboarding/onboarding-wizard.tsx, src/app/(admin)/(painel)/configuracoes/settings-form.tsx, src/app/(admin)/(painel)/produtos/product-form.tsx, src/app/(admin)/(painel)/produtos/size-grid.tsx, src/app/loja/[slug]/[produto]/product-order-panel.tsx</files>
  <action>
Seção 5, item 1 (borda de erro): em TODOS os inputs/selects/textarea com validação nos cinco formulários (login, cadastro, onboarding-wizard, settings-form, product-form), acrescentar `aria-invalid={errors.<campo> ? true : undefined}` no elemento e a utility `aria-invalid:border-[#FF4D4D]` na className (mantendo `border-[#E7F2FD]` como padrão e `focus:border-[#0D21A1]`). No product-form, o campo Preço é um wrapper `<div>` com `focus-within:border-[#E7F2FD]`; ali aplicar a borda de erro condicionalmente via className do wrapper quando `errors.price` existir (`border-[#FF4D4D]` no lugar de `border-[#E7F2FD]`). O texto de erro vermelho existente permanece — a borda é ADIÇÃO, não substituição.

Seção 5, item 2 (distinção primária/secundária dos CTAs) em product-order-panel.tsx: tornar "Pedir agora" dominante — aumentar padding vertical para `py-3`, fonte para `text-base font-semibold` e raio para `rounded-xl`; manter "Copiar pedido" menor (`py-2`, `text-sm`, contorno). Preservar `min-h-11`, o `key`/`animate-shake` e todo o comportamento de href/onClick intactos.

Seção 5, item 3 (pílulas de tamanho) em size-grid.tsx E em product-order-panel.tsx: reforçar o contraste entre "disponível-não-selecionado" e "selecionado/disponível". No estado disponível-não-selecionado usar borda mais visível (`border-[#0D21A1]` com fundo branco e texto `text-[#0D21A1]`); no selecionado manter fundo cheio `bg-[#0D21A1] text-white` e adicionar `font-semibold` + `ring-2 ring-[#0D21A1]/30`. NÃO aumentar os três sinais do estado esgotado; se algo, reduzir para dois (fundo `bg-[#E7F2FD]` + `line-through`), removendo o `opacity-60` redundante. Manter `aria-pressed`, `tabIndex` e handlers.

Seção 5, item 4 (preço na página de detalhe) em product-order-panel.tsx, bloco do título (~linha 236-239): promover o preço para `text-2xl font-bold text-[#111111]` e rebaixar/ajustar o `<h1>` do nome para manter hierarquia (nome grande em cima, preço destacado logo abaixo). O preço NÃO pode continuar em `text-sm` igual a metadado.

Ao final, marcar [x] em `.planning/todos/ajustes-finais-ui-mvp.md` os 4 itens da seção "## 5. Hierarquia visual / feedback de formulário".
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -vE "server-cookies.test" | grep -c "error TS" | grep -qx 0 && grep -c "aria-invalid:border-\[#FF4D4D\]" src/app/\(admin\)/login/page.tsx | grep -qvx 0 && echo OK</automated>
  </verify>
  <done>Inputs de erro mostram borda vermelha via `aria-invalid`; "Pedir agora" é maior/mais pesado que "Copiar pedido"; pílula selecionada é claramente distinta da disponível; preço da página de detalhe está em escala grande; typecheck limpo (exceto erro pré-existente de server-cookies.test); 4 checkboxes da seção 5 marcados.</done>
</task>

<task type="auto">
  <name>Task 2: Identidade de marca em auth/onboarding + seletor de cor curado (Seção 2, itens 1 e 3)</name>
  <files>src/components/vitrino-wordmark.tsx, src/components/accent-color-field.tsx, src/app/(admin)/login/page.tsx, src/app/(admin)/cadastro/page.tsx, src/app/(admin)/esqueci-senha/page.tsx, src/app/(admin)/redefinir-senha/page.tsx, src/app/(admin)/onboarding/onboarding-wizard.tsx, src/app/(admin)/(painel)/configuracoes/settings-form.tsx</files>
  <action>
Seção 2, item 1 (marca nos fluxos de confiança): criar `src/components/vitrino-wordmark.tsx` — um lockup de marca SEM asset de imagem: um badge quadrado `rounded-lg bg-[#0D21A1]` com a inicial "V" em branco (font-bold) ao lado do wordmark "Vitrino" em `text-[#0D21A1] font-bold text-xl`. Componente puro de apresentação, aceita prop opcional `className`. Renderizar `<VitrinoWordmark>` no topo (acima do `<h1>`) das telas login, cadastro, esqueci-senha, redefinir-senha e onboarding-wizard, centralizado, com espaçamento consistente. Copy em português.

Seção 2, item 3 (seletor de cor curado): criar `src/components/accent-color-field.tsx` (client component) que substitui o `<input type="color">` cru. Deve conter: (a) uma fileira de swatches sugeridos como botões clicáveis com as cores da paleta e afins de marca — no mínimo `#0D21A1`, `#000000`, `#111111`, e 3-4 tons complementares curados (ex.: um verde, um vermelho `#FF4D4D`, um roxo/azul-escuro) — cada swatch é um botão `h-9 w-9 rounded-full` com `aria-label` do nome da cor e anel de seleção quando ativo; (b) o `<input type="color">` nativo mantido como opção "outra cor"; (c) um mini-preview mostrando como a cor ficará no hero (um retângulo `rounded-lg` com a cor de fundo e o nome da loja/"Sua loja" em branco). O componente recebe o valor atual e um callback de mudança como props (compatível com react-hook-form via `register`+`setValue` OU via `watch`/`setValue` — passar `setValue` e `watch("accentColor")` do useForm do pai). Substituir o bloco `accentColor` cru tanto em onboarding-wizard.tsx quanto em settings-form.tsx por `<AccentColorField>`. Preservar validação/erro existente do campo.

Ao final, marcar [x] os itens correspondentes da seção "## 2. Identidade visual / design system": o item "Login, cadastro e onboarding são idênticos e sem nenhuma marca..." e o item "Seletor de 'cor de destaque' é o color picker nativo do SO...".
  </action>
  <verify>
    <automated>test -f src/components/vitrino-wordmark.tsx && test -f src/components/accent-color-field.tsx && grep -q "VitrinoWordmark" src/app/\(admin\)/login/page.tsx && grep -q "AccentColorField" src/app/\(admin\)/onboarding/onboarding-wizard.tsx && npx tsc --noEmit 2>&1 | grep -vE "server-cookies.test" | grep -c "error TS" | grep -qx 0 && echo OK</automated>
  </verify>
  <done>Wordmark Vitrino aparece em login/cadastro/esqueci-senha/redefinir-senha/onboarding; seletor de cor tem swatches curados + preview do hero em ambos os formulários; typecheck limpo; 2 checkboxes da seção 2 marcados.</done>
</task>

<task type="auto">
  <name>Task 3: Animação e componentes nativos estilizados (Seção 2, itens 5 e 6)</name>
  <files>src/app/globals.css, src/components/admin-sidebar.tsx, src/app/(admin)/(painel)/produtos/product-list.tsx, src/app/(admin)/(painel)/produtos/product-form.tsx, src/app/(admin)/(painel)/configuracoes/settings-form.tsx</files>
  <action>
Seção 2, item 5 (animação): em globals.css adicionar keyframes e classes reutilizáveis — `fade-in` (opacity 0→1, ~150ms) e `dialog-in` (opacity 0→1 + `scale(.96)`→`scale(1)` + leve translateY, ~180ms ease-out). Aplicar `dialog-in` aos `<dialog>` quando abertos: o drawer de navegação em admin-sidebar.tsx e o diálogo de exclusão em product-list.tsx (usar seletor `dialog[open]` no CSS ou classe utilitária na abertura). Também animar o `::backdrop` com `fade-in`. Garantir respeito a `prefers-reduced-motion: reduce` (envolver as animações num media query que as desliga). NÃO introduzir biblioteca de animação — só CSS.

Seção 2, item 6 (componentes nativos estilizados): criar uma classe utilitária/composição para `<select>` com seta customizada — em globals.css definir uma classe `.select-chevron` que aplica `appearance-none` + um SVG de chevron via `background-image` (data-uri inline, cor `#6B6B6B`), `background-position` à direita e `padding-right` para não sobrepor o texto. Aplicar essa classe a TODOS os `<select>` do app: product-form.tsx (marca, solado, categoria, modalidade, visibilidade) e settings-form.tsx (ocultar esgotados). Manter as classes de borda/foco existentes. Padronizar também a estilização mínima dos `<dialog>` (sombra `shadow-xl`, raio consistente `rounded-xl`) para não variar por navegador.

Ao final, marcar [x] os itens da seção 2: "Animação praticamente inexistente..." e "Componentes nativos não estilizados quebram a coerência...".
  </action>
  <verify>
    <automated>grep -q "dialog-in" src/app/globals.css && grep -q "prefers-reduced-motion" src/app/globals.css && grep -q "select-chevron" src/app/\(admin\)/\(painel\)/produtos/product-form.tsx && npx tsc --noEmit 2>&1 | grep -vE "server-cookies.test" | grep -c "error TS" | grep -qx 0 && echo OK</automated>
  </verify>
  <done>Drawer e diálogo de exclusão abrem com fade/scale (desligado sob reduced-motion); todo `<select>` tem seta customizada consistente; diálogos com sombra/raio padronizados; typecheck limpo; 2 checkboxes da seção 2 marcados.</done>
</task>

<task type="auto">
  <name>Task 4: Consistência de botões e status (Seção 6, itens 1, 2, 3, 4, 7, 11)</name>
  <files>src/components/admin-sidebar.tsx, src/app/(admin)/(painel)/configuracoes/settings-form.tsx, src/app/(admin)/(painel)/configuracoes/qr-code-panel.tsx, src/app/(admin)/(painel)/produtos/product-list.tsx, src/app/(admin)/(painel)/dashboard/page.tsx, src/app/loja/[slug]/[produto]/page.tsx, src/app/loja/[slug]/[produto]/product-order-panel.tsx</files>
  <action>
Item 1 (logout sem loading) em admin-sidebar.tsx: os dois botões "Sair da conta" (sidebar desktop e drawer) são submits de `<form action={signOutAction}>` sem estado pendente. Extrair um pequeno client component `LogoutButton` que usa `useFormStatus()` (do `react-dom`) para desabilitar e trocar o rótulo para "Saindo…" durante o pending, evitando clique duplo. Usar o mesmo componente nos dois lugares (nunca duas implementações).

Item 2 (settings salvar não full-width) em settings-form.tsx: o botão "Salvar alterações" (~linha 192-198) deve virar `w-full` como todo outro CTA primário do painel (adicionar `w-full` à className, preservando o resto).

Item 3 (QR panel dois estilos de botão) em qr-code-panel.tsx: "Baixar PNG" (contorno preto) e "Copiar" (preenchido azul) são ações equivalentes com estilos divergentes. Padronizar: tornar "Baixar PNG" também preenchido `bg-[#0D21A1] text-white` (mesmo estilo do "Copiar"), OU ambos em contorno — escolher o preenchido azul para consistência com o CTA primário do app. Manter ícones e estados disabled.

Item 4 (Publicado vs Rascunho sem cor) em product-list.tsx (~linha 128-130): a pill de status deve distinguir por cor — "Publicado" em `bg-[#0D21A1]/10 text-[#0D21A1]` (ou fundo `bg-[#E7F2FD] text-[#0D21A1]`) e "Rascunho" mantendo o cinza `bg-[#E7F2FD] text-[#6B6B6B]`. Deixar visualmente óbvio qual está publicado.

Item 7 (text-[28px] hardcoded) em dashboard/page.tsx (~linha 85): trocar `text-[28px]` pela escala do Tailwind (`text-3xl`, que é 30px — ou `text-2xl` se preferir manter proporção; usar `text-3xl`). Remover o valor arbitrário.

Item 11 (marca some na página de detalhe): em `src/app/loja/[slug]/[produto]/page.tsx` passar `brand` e `brand_other` (já disponíveis em `detail`) para o `ProductOrderPanel` via a prop `product`. Em product-order-panel.tsx estender o tipo `product` com `brand`/`brand_other`, computar o rótulo de marca (se `brand === "Outra" && brand_other` usar `brand_other`, senão `brand`) e exibi-lo como metadado próximo ao título (ex.: acima do `<h1>` do nome, em `text-sm text-[#6B6B6B]`, possivelmente combinado com a linha).

Ao final, marcar [x] os 6 itens correspondentes da seção "## 6. Inconsistência de design system": logout sem loading, salvar não full-width, QR dois estilos, cor Publicado/Rascunho, text-[28px] hardcoded, e marca some no detalhe.
  </action>
  <verify>
    <automated>grep -q "useFormStatus" src/components/admin-sidebar.tsx && ! grep -q "text-\[28px\]" src/app/\(admin\)/\(painel\)/dashboard/page.tsx && grep -q "brand" src/app/loja/\[slug\]/\[produto\]/product-order-panel.tsx && npx tsc --noEmit 2>&1 | grep -vE "server-cookies.test" | grep -c "error TS" | grep -qx 0 && echo OK</automated>
  </verify>
  <done>Logout mostra "Saindo…" e desabilita durante o pending; "Salvar alterações" é full-width; botões do QR panel têm o mesmo estilo; Publicado/Rascunho distinguem por cor; dashboard usa escala tipográfica do Tailwind; marca aparece na página de detalhe; typecheck limpo; 6 checkboxes da seção 6 marcados.</done>
</task>

<task type="auto">
  <name>Task 5: Consistência de navegação e fluxo (Seção 6, itens 5, 6, 8, 9, 10, 12)</name>
  <files>src/components/store-identity-fields.tsx, src/app/(admin)/onboarding/onboarding-wizard.tsx, src/app/(admin)/(painel)/configuracoes/settings-form.tsx, src/app/(admin)/login/page.tsx, src/app/(admin)/cadastro/page.tsx, src/app/(admin)/esqueci-senha/page.tsx, src/app/loja/[slug]/pagination-numbered.tsx, src/components/admin-sidebar.tsx, src/app/(admin)/(painel)/produtos/product-form.tsx</files>
  <action>
Item 9 (auth usa `<a href>` em vez de `<Link>`): trocar os `<a href="/login">` / `<a href="/cadastro">` por `<Link href=...>` do `next/link` em login/page.tsx, cadastro/page.tsx e esqueci-senha/page.tsx (importar `Link`). Preservar classes e texto.

Item 6 (PaginationNumbered sem números) em pagination-numbered.tsx: o componente só tem Anterior/Próxima — o nome promete números. Adicionar o número da página atual entre os controles: renderizar `Página {currentPage}` (em `text-sm text-[#6B6B6B]`) centralizado entre "Anterior" e "Próxima". (Sem tentar computar total de páginas — não há total disponível; mostrar a página corrente já faz o nome bater com o comportamento.)

Item 8 (onboarding sem indicador de progresso): o "wizard" é tela única. Adicionar um indicador honesto de etapa única — uma pequena legenda/subtítulo abaixo do wordmark do tipo "Etapa única — leva menos de 1 minuto" (`text-xs text-[#6B6B6B]`), alinhando o nome à realidade sem forjar multi-etapas. NÃO transformar em multi-step.

Item 10 (publicar/despublicar sem confirmação) em product-form.tsx: `handleTogglePublish` alterna status com efeito público imediato sem confirmação, enquanto excluir/trocar-slug confirmam. Adicionar um `<dialog>` nativo de confirmação (mesmo padrão do diálogo de exclusão em product-list.tsx): o botão "Publicar"/"Voltar para rascunho" abre o dialog perguntando "Publicar produto na sua vitrine?" / "Tirar produto da vitrine?"; só o botão de confirmação chama a Server Action. Preservar o `useTransition` de publish e os toasts.

Item 12 (nav ativo da sidebar sutil demais) em admin-sidebar.tsx `NavLinks`: o item ativo hoje é `border-l-2` + cor de texto. Padronizar com o resto do app (pílulas cheias): item ativo vira uma pílula `rounded-lg bg-[#E7F2FD] text-[#0D21A1] font-medium` com padding, mantendo `min-h-11` e `aria-current="page"` se já existir (se não existir, não é obrigatório aqui — é item da seção 3). Item inativo permanece texto simples `text-[#6B6B6B]`.

Item 5 (duplicação onboarding-wizard × settings-form): extrair os campos de identidade IDÊNTICOS (Nome da loja, Logo, Cor de destaque via `<AccentColorField>` da Task 2, Frase de apresentação) para `src/components/store-identity-fields.tsx` — client component que recebe `register`, `errors`, `watch`, `setValue` como props e renderiza os quatro blocos de campo. Consumir esse componente em onboarding-wizard.tsx e settings-form.tsx no lugar dos blocos duplicados. NÃO tentar unificar whatsapp/messageTemplate/hideSoldOut (diferem em agrupamento entre as duas telas) — extrair só a parte comprovadamente idêntica. Preservar toda a fiação de react-hook-form e o comportamento de submit de cada tela.

Ao final, marcar [x] os 6 itens correspondentes da seção 6: duplicação onboarding/settings, PaginationNumbered sem números, onboarding sem indicador de progresso, auth com `<a href>`, publicar sem confirmação, e nav ativo sutil.
  </action>
  <verify>
    <automated>test -f src/components/store-identity-fields.tsx && ! grep -q "href=\"/login\"" src/app/\(admin\)/cadastro/page.tsx && grep -q "StoreIdentityFields" src/app/\(admin\)/onboarding/onboarding-wizard.tsx && grep -q "Página" src/app/loja/\[slug\]/pagination-numbered.tsx && npx tsc --noEmit 2>&1 | grep -vE "server-cookies.test" | grep -c "error TS" | grep -qx 0 && echo OK</automated>
  </verify>
  <done>Links de auth usam `<Link>`; paginação mostra a página corrente; onboarding tem legenda de etapa única; publicar/despublicar pede confirmação; nav ativo é pílula cheia; campos de identidade compartilhados via componente único; typecheck limpo; 6 checkboxes da seção 6 marcados.</done>
</task>

<task type="auto">
  <name>Task 6: Tokenização da paleta e fallback do hero (Seção 2, itens 2 e 4)</name>
  <files>src/app/globals.css, src/app/loja/[slug]/store-hero.tsx, e todos os arquivos .tsx/.ts de src/ que contenham hex em className</files>
  <action>
Esta task é a ÚLTIMA — roda depois de todas as edições semânticas para que qualquer hex novo introduzido nas tasks 1-5 também seja tokenizado.

Seção 2, item 4 (tokenizar a paleta): em globals.css, dentro do bloco `@theme inline` existente, definir os tokens de cor de marca (fonte única de verdade):
`--color-brand: #0D21A1;` · `--color-surface: #E7F2FD;` · `--color-danger: #FF4D4D;` · `--color-ink: #111111;` · `--color-muted: #6B6B6B;`
(preto usa o utilitário `black` nativo do Tailwind — não precisa de token). NÃO alterar `--background`, `--foreground`, `--font-*`, nem a regra `body { font-family: Arial... }` (isso é seção 1, fora de escopo).

Migração mecânica: substituir, em TODO src/, as ocorrências de valor arbitrário entre colchetes pelos nomes de token — `[#0D21A1]→brand`, `[#E7F2FD]→surface`, `[#FF4D4D]→danger`, `[#111111]→ink`, `[#6B6B6B]→muted`, `[#000000]→black`. Como só utilities Tailwind usam a forma `[#hex]` com colchetes, isso converte `bg-[#0D21A1]→bg-brand`, `text-[#111111]→text-ink`, `border-[#E7F2FD]→border-surface`, `text-[#FF4D4D]→text-danger`, `hover:bg-[#FF4D4D]/10→hover:bg-danger/10`, `focus:border-[#0D21A1]→focus:border-brand`, etc. IMPORTANTE: strings JS sem colchetes (ex.: `defaultValues.accentColor = "#0D21A1"`, `style={{ backgroundColor: ... }}`, o data-uri do chevron em globals.css, e o bloco `@theme` que acabou de ser definido) NÃO devem ser tocadas — a substituição é APENAS da forma `[#hex]`. Pode ser feito com um sed sobre os arquivos que casam, mas confira que nenhum literal JS/CSS fora de className foi alterado.

Seção 2, item 2 (fallback do hero desconectado): em store-hero.tsx, o fallback `style={{ backgroundColor: store.accentColor ?? "#000000" }}` usa preto puro, divergindo do azul de marca usado como cor de ação. Trocar o fallback para o azul de marca: `?? "#0D21A1"` (mantém coerência de "cor de marca" única quando a loja não configurou `accent_color`). Este é um literal JS, editado à mão (não pela migração de colchetes).

Verificação de que a paleta não mudou de valor: os tokens em `@theme` devem conter exatamente os seis hex oficiais; nenhum hex novo pode ser introduzido.

Ao final, marcar [x] os itens da seção 2: "Hero da vitrine pública usa preto puro (#000000) como fallback..." e "Paleta 100% em hex cru repetido centenas de vezes...".
  </action>
  <verify>
    <automated>! grep -rIE "\[#(0D21A1|E7F2FD|FF4D4D|111111|6B6B6B|000000)\]" src && grep -q "\-\-color-brand: #0D21A1" src/app/globals.css && grep -q '?? "#0D21A1"' src/app/loja/\[slug\]/store-hero.tsx && npm run build 2>&1 | tail -5 | grep -qiE "compiled|success|generating|✓" && echo OK</automated>
  </verify>
  <done>Zero utilities `[#hex]` em src/; tokens `brand/surface/danger/ink/muted` definidos em @theme com os hex oficiais; fallback do hero é azul de marca; `npm run build` compila; 2 checkboxes da seção 2 marcados. Total: 22/22 itens das seções 2, 5, 6 marcados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Task exclusivamente de UI/estilo: nenhuma nova entrada não confiável, nenhuma mudança de RLS/auth/dados, nenhum novo endpoint. A rota pública `/loja/[slug]` permanece sem middleware de auth (não alterada). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0z2-01 | Tampering | migração mecânica de hex→token | low | mitigate | Verify da Task 6 garante que só a forma `[#hex]` foi trocada e que `npm run build` compila; literais JS/CSS preservados. |
| T-0z2-SC | Tampering | instalação de pacotes | low | accept | Nenhuma dependência nova é instalada (Tailwind v4 + libs já presentes) — sem superfície de supply-chain nesta task. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo (exceto o erro pré-existente já documentado em `tests/setup`/`server-cookies.test.ts`).
- `npm run build` compila sem erros.
- `grep -rIE "\[#(0D21A1|E7F2FD|FF4D4D|111111|6B6B6B|000000)\]" src` retorna vazio (paleta tokenizada).
- Os 22 checkboxes das seções 2, 5 e 6 de `.planning/todos/ajustes-finais-ui-mvp.md` estão `[x]`; seções 1, 3, 4, 7, 8, 9 permanecem `[ ]`.
- Nenhuma cor fora da paleta oficial foi introduzida.
</verification>

<success_criteria>
- Marca Vitrino visível em login, cadastro, esqueci-senha, redefinir-senha e onboarding.
- Feedback de erro de formulário inclui mudança de cor de borda; "Pedir agora" domina "Copiar pedido"; preço destacado na página de detalhe.
- Paleta tokenizada como fonte única de verdade via `@theme`, sem alterar os valores hex oficiais.
- Consistência de botões/status/navegação corrigida (logout loading, full-width, QR, status colorido, paginação numerada, nav em pílula, confirmação de publicação, marca no detalhe).
- 22/22 itens das seções 2, 5, 6 marcados; escopo das demais seções intacto.
- `npm run build` verde.
</success_criteria>

<output>
Quick task — sem SUMMARY.md dedicado exigido pelo fluxo de fase. O commit final deve referenciar os itens do checklist marcados. Atualizar `.planning/todos/ajustes-finais-ui-mvp.md` com os 22 checkboxes `[x]` das seções 2, 5, 6 (anotando commit/data conforme a legenda do documento).
</output>
