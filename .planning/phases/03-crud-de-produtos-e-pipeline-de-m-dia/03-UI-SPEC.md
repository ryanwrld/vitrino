---
phase: 3
slug: crud-de-produtos-e-pipeline-de-m-dia
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-13
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — native HTML elements + Tailwind CSS v4 utility classes, React Hook Form for form state (established Phase 1/2 pattern) |
| Icon library | `lucide-react` (already installed since Phase 2, gate-approved) — this phase adds: `Plus`, `Search`, `SlidersHorizontal`, `ArrowUpDown`, `Pencil`, `Trash2`, `GripVertical`, `ImageOff`, `Loader2`, `Check`, `X`, `CircleSlash` |
| Font | Geist Sans (`--font-geist-sans`, inherited from Phase 1 root `layout.tsx`) — no new font loading this phase |

**Design system decision (no shadcn — continuing established precedent):** `components.json` does not exist. Phase 2 explicitly decided against introducing shadcn mid-project ("continues the established hand-rolled pattern for consistency... revisit as a deliberate, dedicated decision in a future phase, do not introduce it silently"). Phase 3 has more UI surface than Phase 2 (multi-file uploader, drag-and-drop, size grid, filters), but none of that changes the calculus — introducing a component library now would fork the visual system for a single phase while three prior phases (auth, onboarding, settings) remain hand-rolled. This UI-SPEC continues native HTML + Tailwind, matching `onboarding-wizard.tsx` / `settings-form.tsx` / `slug-editor.tsx` conventions exactly (same input/label/button classes, same `<dialog>`-based confirmation pattern). Not re-litigated here per the auto-mode bias toward continuing an already-made, still-valid decision rather than re-asking.

**Known drift (not this phase's job to fix):** `PROJECT.md` specifies Syne 700 (display/headings) + Inter 400/500 (body) as target brand typography, and a 12/14/16/20/28/40px scale — but the codebase has shipped Geist Sans (Next.js scaffold default) since Phase 1 and never loaded Syne/Inter. This UI-SPEC follows the *implemented* reality to stay visually consistent with `/dashboard`, `/configuracoes`, `/onboarding`. Flag for a future dedicated typography pass; do not fix unilaterally inside this phase's plans.

---

## Spacing Scale

Declared values (must be multiples of 4), matching the established Phase 1/2 usage (`gap-1`, `gap-4`, `gap-6`, `px-4 py-10`, `px-3 py-2`):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Label-to-input gap (`gap-1`), icon-to-text gap inside status/availability pills, gap between size-grid pills |
| sm | 8px | Compact spacing inside a field group, gap between filter/sort controls in the toolbar |
| md | 16px | Default gap between form fields (`gap-4`), input horizontal padding (`px-4`), gap between photo-uploader slots |
| lg | 24px | Gap between form sections — Identificação / Solado & Categoria / Preço / Tamanhos / Fotos / Descrição (`gap-6`) |
| xl | 32px | Gap between the page header (title + "Novo produto" CTA) and the product list/toolbar |
| 2xl | 48px | Reserved, not used this phase |
| 3xl | 64px | Reserved, not used this phase |

Exceptions (mobile-first, per `CLAUDE.md`):
- Every tappable element that isn't full-text (size-grid pill, photo remove "×", drag handle, icon-only filter/sort buttons) must have a minimum 44×44px touch target, even if the visible glyph is smaller — pad with `p-3`/`min-h-11 min-w-11`, not just icon size.
- The photo-uploader drag handle (`GripVertical`) needs an especially generous hit area (`p-2` minimum around the icon) since it is the sole affordance for reordering on touch devices — a cramped handle makes D-12 unusable on mobile.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (`text-base`) | 400 (regular) | 1.5 |
| Label | 14px (`text-sm`) | 500 (medium) | 1.4 |
| Heading (section) | 20px (`text-xl`) | 500 (medium) | 1.3 |
| Display (page title) | 24px (`text-2xl`) | 700 (bold) — single-use exception, inherited from Phase 1/2's `<h1>` pattern | 1.2 |

Notes:
- Two core weights govern this phase's UI: 400 (body text, product descriptions, hint/caption text, size-grid pill labels) and 500 (form labels, buttons, section headings — "Identificação", "Tamanhos", "Fotos", price value on product cards). Weight 700 is reserved exclusively for the page-level `<h1>` ("Produtos", "Novo produto", "Editar produto") — do not use bold anywhere else in this phase, including product-card names or prices.
- Caption text (photo-slot hint "até 5MB", size-grid legend, empty-state body, product-count summary) reuses the Phase 1/2 pattern: `text-xs` (12px) at 400 weight, color `#6B6B6B`. This is a caption variant of Body, not a 5th declared size.
- Price display on product cards/list rows uses Label size+weight (14px/500), never a larger/bolder treatment — keeps the name as the visual anchor of each row, consistent with "no new weight" discipline above.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` | Page background, form container background, product list background (matches `bg-white` on every existing admin page) |
| Secondary (30%) | `#E7F2FD` | Input/select borders, section dividers, empty photo-slot dashed background, "esgotado" pill fill, "rascunho" status badge fill, disabled input backgrounds |
| Accent (10%) | `#0D21A1` | Reserved for: primary CTA buttons ("Novo produto", "Salvar produto"), input/select focus border (`focus:border-[#0D21A1]`), "disponível" size-pill fill, "publicado" status badge fill, save-success toast accent |
| Destructive | `#FF4D4D` | Reserved for: delete-product confirm button, field validation error text, photo-upload error text (>5MB, >5 photos, wrong format), "esgotado produto inteiro" is **not** this color (see note below) |

Accent reserved for: "Novo produto" button, "Salvar produto" button, active input/select focus ring, "disponível" size pills, "Publicado" status badge, publish-toast success state. Never apply accent blue to purely decorative or non-interactive elements, and never to the "esgotar produto inteiro" shortcut (that is a neutral/negative-state action, not a success action — see below).

**Important distinction — "esgotado" is a neutral state, not an error state:** per `PROJECT.md`'s bug catalog ("pills de tamanho esgotado precisam de visual riscado"), esgotado sizes use the *secondary* gray (`#E7F2FD` fill / `#6B6B6B` text + `line-through`), never the destructive red. Red is reserved strictly for actual errors and the one truly destructive action (deleting a product). This keeps the accent/destructive reservation lists honest — most of this phase's negative-looking states (esgotado, rascunho) are muted grays, not red or blue.

Additional ink token (not a surface, carried from Phase 1/2 — required for parity with existing pages): `#000000` (black) for page `<h1>` text color and secondary/outline button borders+text (e.g. "Cancelar" in dialogs, "Voltar" links). Body copy/labels use `#111111` (near-black); `#6B6B6B` (medium gray) for secondary/hint/caption text and the "rascunho"/"esgotado" states described above.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (product list page) | "Novo produto" |
| Primary CTA (product form submit) | "Salvar produto" |
| Empty state heading (no products at all) | "Nenhum produto cadastrado ainda" |
| Empty state body (no products at all) | "Cadastre seu primeiro produto para começar a vender pelo WhatsApp." |
| Empty state heading (search/filter with zero matches) | "Nenhum produto encontrado" |
| Empty state body (search/filter with zero matches) | "Tente ajustar os filtros ou buscar por outro termo." |
| Error state (generic save failure) | "Não foi possível salvar o produto. Verifique sua conexão e tente novamente." |
| Error state (photo over 5MB) | "Essa foto passou do limite de 5MB." |
| Error state (6th photo attempted) | "Você já atingiu o limite de 5 fotos por produto." |
| Error state (invalid file type) | "Envie apenas fotos em JPG, PNG ou WEBP." |
| Destructive confirmation (delete product) | Title: "Excluir {nome do produto}?" — Body: "Isso vai remover o produto e todas as fotos da sua vitrine. Essa ação não pode ser desfeita." — Confirm button: "Sim, excluir" — Cancel button: "Cancelar" |

Additional interaction copy specific to this phase (required for the size-grid, photo pipeline, publish/draft, and bulk-esgotado contracts described in Layout & Interaction Notes below):

| State | Copy |
|-------|------|
| Page title (list) | "Produtos" |
| Page title (create) | "Novo produto" |
| Page title (edit) | "Editar produto" |
| Search input placeholder | "Buscar por nome…" |
| Filter: status | "Todos" / "Publicado" / "Rascunho" |
| Filter: marca | "Todas as marcas" + brand list (see Layout Notes) |
| Filter: solado | "Todos os solados" + FG/AG/TF/IC/MG/SG |
| Sort dropdown | "Mais recente" / "Nome" / "Preço" |
| Product-count summary | "{N} produtos" (or "1 produto" singular) |
| Status badge — draft | "Rascunho" |
| Status badge — published | "Publicado" |
| Publish button (on a draft product) | "Publicar" |
| Unpublish button (on a published product) | "Voltar para rascunho" |
| Size-grid section legend | "Toque em um tamanho para adicioná-lo. Toque de novo para marcar disponível." |
| Size pill — not included (tap to add) | Size number only, muted outline, e.g. "36" |
| Size pill — included, esgotado (default) | Size number, struck through, e.g. "~~38~~" |
| Size pill — included, disponível | Size number, solid blue, e.g. "38" |
| Bulk shortcut button | "Marcar tudo como esgotado" |
| Photo section legend | "Até 5 fotos. A primeira é a capa da sua vitrine." |
| Photo slot — empty | "+" icon, caption "Adicionar foto" |
| Photo slot — uploading | Spinner overlay, caption "Enviando…" |
| Photo slot — cover badge (position 1) | "Capa" |
| Toast: product created | "Produto salvo!" |
| Toast: product updated | "Produto atualizado!" |
| Toast: product deleted | "Produto excluído." |
| Toast: product published | "Produto publicado!" |
| Toast: product unpublished | "Produto movido para rascunho." |
| Toast: size availability toggled | "Disponibilidade atualizada." |
| Toast: all sizes marked esgotado | "Todos os tamanhos marcados como esgotado." |
| Toast: photo removed | "Foto removida." |
| Toast: photo reorder saved | (silent — no toast; reorder is optimistic and low-stakes, matches D-12's "easy, no re-upload" intent) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none — shadcn not initialized this phase | not applicable |
| third-party | none | not applicable |

---

## Layout & Interaction Notes

(Supplementary — derived directly from `03-CONTEXT.md` decisions D-01 through D-13 and `03-RESEARCH.md` schema/pipeline patterns; included so the executor has one single visual source of truth alongside the checker's 6 dimensions above.)

### Routes
- `/produtos` — list page (Server Component, `searchParams`-driven filter/search/sort per Pattern 3 of RESEARCH).
- `/produtos/novo` — create form (single screen, D-08).
- `/produtos/[id]/editar` — edit form, same component as create with pre-filled `defaultValues`.
- All three routes sit inside the `(admin)` route group behind the existing `requireCompletedOnboarding()` guard.

### Product list page (`/produtos`)
- Header row: `<h1>` "Produtos" (Display style, `#000000`) + "Novo produto" button (accent, top-right on desktop, full-width below the title on mobile — matches the mobile-first stacking already used in `settings-form.tsx`).
- Toolbar row below the header: search input (`Search` icon prefix, debounced via existing `useDebouncedValue`) + status/marca/solado filter selects + sort dropdown. On mobile, stack these into a wrapped flex row (`flex-wrap gap-2`), never a horizontal-scroll strip that hides controls.
- Each product row/card shows: cover thumbnail (position-1 photo, 64×64px rounded, `#E7F2FD` background) or a placeholder icon (`ImageOff`, `#6B6B6B`) when no photo exists yet; name (Body, 500 weight) + brand/line as a secondary line (Caption, `#6B6B6B`); price (Label size/weight); status badge (Rascunho/Publicado); availability indicator derived from `EXISTS` over `product_sizes` (per RESEARCH Pattern 1) — "Disponível" (blue dot + text) or "Esgotado" (gray dot + text, no strikethrough at this rollup level, strikethrough is reserved for individual size pills); edit (`Pencil`) and delete (`Trash2`) icon buttons, both meeting the 44×44px touch target minimum.
- Empty states: distinguish "no products exist at all" from "filters/search matched nothing" using the two separate copy pairs declared above — do not show the same empty state for both, since the fix differs (create vs. clear filters).

### Product form (`/produtos/novo`, `/produtos/[id]/editar`) — single screen, D-08
Sections, in this order, each with a section heading (Heading style) and `gap-6` between sections, `gap-4` between fields within a section — matches `settings-form.tsx`'s two-section pattern extended to six:

1. **Identificação** — nome (required), marca (required, `<select>` from fixed list below + "Outra" revealing a text input), linha (optional, free text).
2. **Solado & Categoria** — solado (`<select>`, optional): FG, AG, TF, IC, MG, SG; categoria (`<select>`, optional): Chuteira, Tênis, Chinelo, Outro; modalidade (`<select>`, optional): Sob encomenda, Pronta entrega, Ambos.
3. **Preço** — required, `type="text"` with `inputMode="decimal"` (not `type="number"`, to avoid native spinner/locale conflicts with BRL comma-decimal input per RESEARCH Pitfall 3), prefixed with "R$" as static text inside the input's padding, placeholder "0,00".
4. **Tamanhos** — the size grid (see dedicated subsection below) + the bulk "Marcar tudo como esgotado" shortcut button (outline style, `#6B6B6B` border/text, not accent/destructive) positioned directly below the grid.
5. **Fotos** — the photo uploader (see dedicated subsection below).
6. **Descrição** — optional `<textarea>`, same visual treatment as the onboarding message-template field (`rows={4}`, `text-sm`).

Submit button "Salvar produto" (accent, full-width on mobile) pinned at the bottom of the form, same disabled/pending pattern as existing forms (`disabled={isPending}`, label swaps to "Salvando…"). On the edit route only, also render "Publicar"/"Voltar para rascunho" as a secondary button next to (not replacing) "Salvar produto" — publishing is a distinct action from saving field edits, per D-10.

**Required-field visual distinction (D-09):** nome/marca/preço labels get no special marker beyond the existing error-on-submit pattern already used project-wide (red inline text under the field) — do not add asterisks or a "campos obrigatórios" legend that doesn't exist elsewhere in this codebase; consistency with `onboarding-wizard.tsx` (which also has only 2 required fields and no asterisk convention) takes precedence over inventing a new pattern for this phase alone.

### Size grid (D-01–D-04)
- Grid of 10 pills, sizes 36–45, `grid-cols-5 gap-2` on mobile (two rows of five) or a single `flex flex-wrap gap-2` row on wider viewports.
- Each pill is a `<button type="button">`, minimum 44×44px, rounded-lg, showing only the size number.
- **Three-state single-tap cycle** (mobile-first — no long-press, no separate delete affordance, no ambiguity):
  1. **Not included** → outline only (`border-[#E7F2FD] text-[#6B6B6B]`, no fill). Tap → becomes "included, esgotado" (D-03: newly-included sizes always start esgotado, never disponível-by-default).
  2. **Included, esgotado** → filled `#E7F2FD` background, `#6B6B6B` text, `line-through` (mirrors the project's existing "riscado" convention for esgotado sizes on the public storefront, applied here in the admin panel too for visual consistency). Tap → becomes "included, disponível".
  3. **Included, disponível** → filled `#0D21A1` background, white text, no strikethrough. Tap → cycles back to "not included" (removes the size from the product).
- On product creation, sizes 37–43 initialize at state 2 (included, esgotado — D-02/D-03); sizes 36, 44, 45 initialize at state 1 (not included — D-02).
- The bulk shortcut "Marcar tudo como esgotado" moves every currently-included pill to state 2 in one action; it never adds sizes that are in state 1 (it only affects sizes the revendedor already chose to include) — this matches RESEARCH's schema resolution (`UPDATE product_sizes SET available = false WHERE product_id = X`, which only touches existing rows).

### Photo uploader (D-11–D-13, PROD-03)
- Grid of exactly 5 square slots, `grid-cols-5 gap-2` on mobile wrapping if needed, aspect-square, rounded-lg.
- **Empty slot:** dashed `#E7F2FD` border, centered `Plus` icon + "Adicionar foto" caption (`text-xs text-[#6B6B6B]`), tapping opens the native file picker (`accept="image/png,image/jpeg,image/webp"`).
- **Filled slot:** thumbnail image fills the slot; a small "Capa" badge (pill, `#0D21A1` background, white text, `text-xs`) appears only on position 1 (D-11 — no separate "set as cover" action exists, this badge is purely informational); a drag handle (`GripVertical`, top-left, `p-2` hit area) enables reordering via `@dnd-kit/sortable`; a remove "×" button (top-right, `p-2` hit area, `#FF4D4D` icon on white circular background) removes that single photo and reverts the slot to empty, accepting a new upload immediately (D-13 — never disturbs the other slots).
- **Uploading state:** semi-transparent dark overlay on the thumbnail-in-progress with a centered `Loader2` spinner + "Enviando…" caption — this is the "feedback de progresso" PROD-03 requires; do not let the slot look inert during compression/upload.
- **Compression happens before the slot shows "Enviando…" completes** — the client compresses via `browser-image-compression` first (silent, sub-second for most phone photos), then the network upload shows the spinner; do not show two separate progress states (compress vs. upload) — one unified "Enviando…" covers both, keeping the UI simple for a non-technical user.
- Drag-and-drop only reorders among filled slots; empty slots are not drop targets and do not participate in the sortable context.

### Confirmation dialog (delete product)
- Reuses the exact native `<dialog>` pattern from `slug-editor.tsx`: `rounded-lg p-6 backdrop:bg-black/40`, title (Heading style, `#111111`), body (`text-sm text-[#6B6B6B]`), two-button footer — "Cancelar" (outline, `#000000` border/text) + "Sim, excluir" (filled `#FF4D4D`, white text, disabled+"Excluindo…" while pending).
- Triggered only by the `Trash2` icon button on the list row (or an equivalent button on the edit form) — never by any other action. Confirm handler calls the delete Server Action directly (`onClick`, not `method="dialog"` submit) so the dialog can show a pending state before closing, matching the slug-editor's `handleConfirm` pattern exactly.
- Publish/unpublish and the bulk "esgotar tudo" shortcut do **not** get a confirmation dialog — both are reversible, low-stakes toggles (unlike the slug change or product delete, which are genuinely destructive/breaking). Do not over-apply the confirmation pattern to actions that don't warrant it.

### Fixed lists (UI copy — content owned by this spec, final enumeration confirmable by planner per RESEARCH Open Question 1)
- **Marca** (D-05, `<select>` + "Outra" reveals free-text input): Nike, Adidas, Puma, Mizuno, Under Armour, New Balance, Umbro, Outra.
- **Solado** (D-07, fixed): FG, AG, TF, IC, MG, SG.
- **Categoria** (RESEARCH Open Question 1 recommendation, adopted here as the UI default): Chuteira, Tênis, Chinelo, Outro.
- **Modalidade**: Sob encomenda, Pronta entrega, Ambos.

### Primary visual anchor
The "Novo produto" button on the list page and "Salvar produto" on the form are the highest-contrast CTAs (accent blue `#0D21A1`) on their respective screens — largest interactive elements after page titles. Everything else (filters, sort, secondary publish toggle, bulk esgotado shortcut, photo remove/drag controls) is visually subordinate (outline or icon-only, neutral colors), so the accent blue never competes with itself across more than one element per screen.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
