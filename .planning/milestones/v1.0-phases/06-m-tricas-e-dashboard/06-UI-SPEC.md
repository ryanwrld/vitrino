---
phase: 6
slug: metricas-e-dashboard
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-15
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual Tailwind v4 (CSS-first, no `tailwind.config.js`), no component library initialized (`components.json` absent) |
| Preset | not applicable |
| Component library | none — every interactive element (stat cards, sidebar, drawer, list rows) is hand-rolled with `<aside>`/`<nav>`/`<dialog>`/`<button>`/`<a>` + Tailwind utility classes, identical convention to `product-list.tsx`, `product-filters.tsx`, `size-grid.tsx`. The mobile nav drawer uses the **native `<dialog>` element** (`.showModal()`), per `06-RESEARCH.md` Pattern 5 — gives focus-trap, Escape-to-close and `::backdrop` for free, same primitive already used by `slug-editor.tsx`'s confirmation dialog and `product-list.tsx`'s delete dialog |
| Icon library | `lucide-react` 1.24.0 (already installed, legitimacy-audited in Phase 2 — see STATE.md "lucide-react aprovado no gate de legitimidade (T-02-SC)"). New icons this phase: `Menu`/`X` (hamburger/close), `Package` (total produtos), `CheckCircle2` (disponíveis), `XCircle` (esgotados), `Eye` (acessos/mais visualizados), `MessageCircle` (cliques WhatsApp) |
| Font | System stack via `globals.css` (`font-family: Arial, Helvetica, sans-serif` on `body`) — no new font added this phase. Note: `next/font/google` loads Geist Sans/Mono into CSS variables in `layout.tsx`, but `body`'s explicit `font-family` in `globals.css` currently overrides them, so the *rendered* font project-wide is the system Arial/Helvetica stack, not Geist or the Syne/Inter pair described in PROJECT.md's aspirational identity section — same deviation already documented in `05-UI-SPEC.md`, not reopened here |

**Note on shadcn gate:** This is the final phase of the v1.0 milestone (6/6). Five prior phases built a complete, internally consistent hand-rolled Tailwind design language (verified directly from `product-list.tsx`, `product-form.tsx`, `size-grid.tsx`, `store-hero.tsx`, `product-order-panel.tsx`). Introducing shadcn now would fragment the design system for zero remaining phases of benefit. **Decision: continue with the existing hand-rolled convention, `Tool: none`.** Registry Safety section below is therefore not applicable.

---

## Spacing Scale

Declared values (must be multiples of 4) — matches the scale already in use project-wide:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gaps (stat card icon + label, availability dot + label), `gap-1` |
| sm | 8px | Compact stacks — stat card internal gaps, Top-10 list row internal gaps (`gap-2`) |
| md | 12px–16px | Default spacing — sidebar nav link vertical rhythm (`gap-3`), section padding (`p-4`), card padding |
| lg | 24px | Section-to-section gaps on the dashboard page (stat grid → recentes → mais visualizados → cliques WhatsApp), sidebar internal padding |
| xl | 32px | Gap between the sidebar/drawer nav block and the "Sair da conta" footer |

Exceptions:
- **44px (`min-h-11`/`min-w-11`) touch targets** — mandatory for the hamburger button, the drawer close button, every sidebar/drawer nav `<Link>` row, and the "Sair da conta" button. Established project-wide exception (`size-grid.tsx`, `product-list.tsx` action buttons), required here specifically because the drawer is the *only* way to reach Produtos/Configurações on mobile — a mis-tap here blocks navigation entirely.
- **Sidebar fixed width `w-56` (224px) / drawer fixed width `w-64` (256px)** — not part of the 8-point spacing scale (component sizing, not spacing), called out here because both dimensions are locked by `06-RESEARCH.md` Pattern 5's reference implementation and must not drift between desktop/mobile.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (`text-base`) | Regular (400) | 1.5 (`leading-normal`) — sidebar/drawer nav link labels, Top-10 list product names, "produtos recentes" row text |
| Label | 14px (`text-sm`) | Medium (500) | 1.5 (`leading-normal`) — stat card labels ("Total de produtos", "Disponíveis", "Esgotados", "Acessos"), Top-10 list count text, section sub-labels |
| Heading | 20px (`text-xl`) | Medium (500) | 1.2 (`leading-tight`) — section titles: "Produtos recentes", "Mais visualizados", "Cliques no WhatsApp"; mirrors the existing `text-xl font-medium` convention used by every other admin section header (`size-grid.tsx`, `product-form.tsx`, `settings-form.tsx`) |
| Display | 28px (`text-[28px]`) | Bold (700) | 1.2 (`leading-tight`) — the big number inside each stat card (total/disponível/esgotado/acessos count). First component in the codebase to use PROJECT.md's declared 28px scale step (previously only 12/14/16/20/24px were in use) — introduced here because a metrics dashboard is the first surface where a number itself is the primary content, and 24px (the existing page-`<h1>` size) would visually compete with the page title instead of standing out as data |

Page `<h1>` ("Dashboard"): reuses the pre-existing project-wide convention verbatim — `text-2xl font-bold text-[#000000]` (24px/700), same as every other admin page title (`Produtos`, `Configurações`). Not part of the "Display" role above; this is the established page-title pattern, unchanged.

Weight discipline: only **2 weights used in this phase's new components** — Regular (400, nav labels/list body text) and Medium (500, labels/section headings). Bold (700) is reused only where the existing project convention already uses it: the page `<h1>` and — newly, deliberately — the stat-card Display numbers (justified above), never on any other new element in this phase.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` | Page background of `/dashboard` and every `(painel)` route; sidebar background; drawer background; stat card background |
| Secondary (30%) | `#E7F2FD` | Stat card border, sidebar `border-r`, list-row dividers/borders (Top-10 rows, "produtos recentes" rows), empty-state dashed border |
| Accent (10%) | `#0D21A1` | **Reserved for:** the "Disponíveis" stat card's number + icon (matches the existing blue "disponível" dot convention from `product-list.tsx`); the active sidebar/drawer nav link's text color + a 2px left border indicator bar |
| Destructive | `#FF4D4D` | Not introduced by this phase — no new destructive action exists here (dashboard is read-only metrics + navigation). Reused unchanged by the pre-existing product delete flow (`product-list.tsx`), which this phase does not touch |

Accent reserved for: **"Disponíveis" stat number/icon, active sidebar/drawer nav link (text + left indicator bar).** Never applied to "Total de produtos" or "Acessos" (neutral, see below), never applied to "Esgotados" (muted gray, see below — explicitly NOT red, matching the established `product-list.tsx` precedent where the product-level "Esgotado" indicator uses `#6B6B6B`, not `#FF4D4D`), never applied to inactive nav links, never applied to Top-10 list rows (those are neutral/informational, not calls to action).

Additional tokens already established project-wide, reused (not redefined) in this phase:

| Role | Value | Usage |
|------|-------|-------|
| Text primary | `#111111` | Stat numbers for "Total de produtos" and "Acessos" (neutral counts — neither positive nor negative), Top-10 list product names, "produtos recentes" row titles |
| Text secondary/muted | `#6B6B6B` | "Esgotados" stat number + icon (muted, not red — see Accent note above), stat card labels, inactive sidebar/drawer nav links, Top-10 list count text, empty-state body copy |
| Brand black | `#000000` | Page `<h1>` ("Dashboard"), "Total de produtos" stat icon (neutral-brand tone, distinct from the accent blue used for "Disponíveis") |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | **"Novo produto"** — verbatim reuse of the existing button label from `produtos/page.tsx` (never re-worded to "Cadastrar produto" or similar), `href="/produtos/novo"`. Appears in the dashboard's zero-product empty state |
| Sidebar nav items | **"Dashboard"**, **"Produtos"**, **"Configurações"** — locked verbatim by `06-CONTEXT.md` D-07 |
| Sidebar footer action | **"Sair da conta"** — verbatim reuse of the existing button label from `dashboard/page.tsx`'s current placeholder; calls the existing `signOutAction`, never recreated. Styled neutrally (not destructive red) — logout is not a destructive action |
| Stat card labels | **"Total de produtos"**, **"Disponíveis"**, **"Esgotados"**, **"Acessos"** — locked verbatim by ROADMAP Phase 6 Success Criteria #2 / REQUIREMENTS MTR-02 |
| Section headings | **"Produtos recentes"**, **"Mais visualizados"**, **"Cliques no WhatsApp"** |
| Empty state (zero products in the store) | Heading **"Nenhum produto cadastrado ainda"**, Body **"Cadastre seu primeiro produto para começar a vender pelo WhatsApp."** — verbatim reuse of the existing zero-state copy from `produtos/page.tsx`, plus the "Novo produto" CTA above. Applies to the "Produtos recentes" section when the store has 0 products; when this fires, the stat cards still show numeric `0`s (not a second empty-state message — see below) |
| Empty state (stat cards, zero data) | Stat cards **always render the numeric `0`**, never a text empty-state substitute — a brand-new store with 0 products/0 accesses is a normal, expected state, and swapping a number for prose would be inconsistent across 4 cards on one row/grid |
| Empty state ("Mais visualizados", products exist but 0 pageviews yet) | Heading **"Ainda sem visualizações"**, Body **"Assim que sua vitrine receber acessos, os produtos mais vistos aparecem aqui."** |
| Empty state ("Cliques no WhatsApp", products exist but 0 order clicks yet) | Heading **"Ainda sem cliques"**, Body **"Assim que clientes clicarem em \"Pedir agora\", os produtos mais pedidos aparecem aqui."** |
| Error state | N/A — this phase introduces no new user-input surface and no custom `error.tsx` (no phase in this project has added one; dashboard queries are bare owner-scoped reads). Falls back to the Next.js default error boundary, consistent with the rest of the admin panel |
| Destructive confirmation | N/A — no destructive action is introduced by this phase. The existing product delete confirmation dialog (`product-list.tsx`, unchanged) remains the only destructive-action UI in the admin panel |

---

## Component Specifications

*(Supplementary detail beyond the template's minimum — included because this phase introduces two structurally new component families: the sidebar/drawer shell and the stat-card/Top-10-list dashboard body.)*

### Stat card (4 instances: Total de produtos, Disponíveis, Esgotados, Acessos)
- Container: `rounded-lg border border-[#E7F2FD] bg-white p-4 flex flex-col gap-1`.
- Icon (top, `h-5 w-5`, color per the Color table above — `#000000` neutral / `#0D21A1` accent for Disponíveis / `#6B6B6B` muted for Esgotados / `#000000` for Acessos) + Label (14px/500, `#6B6B6B`) on one row; Display number (28px/700, color per role) below.
- Layout: mobile-first `grid grid-cols-2 gap-3` (2 columns × 2 rows on mobile); `md:grid-cols-4 md:gap-4` (single row on desktop). Matches the mobile-first grid discipline already used by the public product grid (`loja/[slug]/page.tsx`).
- Not clickable/interactive — pure display, no hover/focus states needed (no `<button>`/`<a>` wrapper).

### Top-10 list (2 instances: Mais visualizados, Cliques no WhatsApp — always rendered as two separate, parallel lists per D-08/D-09, never merged)
- Row: `flex items-center gap-3 rounded-lg border border-[#E7F2FD] bg-white p-3`, mirroring `product-list.tsx`'s existing row shell.
- Left: rank number (`1`–`10`), 14px/500, `#6B6B6B`, fixed-width `w-5` for alignment.
- Middle: product name (16px/400, `#111111`, `truncate`), optionally the brand/line secondary line (12px, `#6B6B6B`) exactly as in `product-list.tsx`'s `secondaryLine` pattern.
- Right: count (14px/500), with the metric's icon (`Eye` for views, `MessageCircle` for clicks) at `h-4 w-4`, `#6B6B6B` — never accent-colored (these are informational counts, not calls to action; see Color contract).
- Each row is a `<Link href="/produtos/[id]/editar">` (whole row clickable, `min-h-11` for touch), reusing the navigation pattern already established for editing a product from a list.
- Truncated to exactly 10 rows (`limit(10)` at the query layer per `06-RESEARCH.md`) — no "ver mais" pagination for this phase.

### "Produtos recentes" list
- Same row shell as the Top-10 list above (thumbnail via `coverUrl`/`ImageOff` fallback reused verbatim from `product-list.tsx`, name, brand/line secondary text, price via `formatBRLPrice`, status dot).
- Shows the **5 most recent products** (`created_at desc`, `slice(0, 5)` of the same `queryProducts` result already used for the stat counts — per `06-RESEARCH.md` "Don't Hand-Roll" §1, zero new SQL). Five, not ten, to keep this section visually secondary to the two Top-10 metric lists on a single mobile-first page (D-04: everything lives on one page — five rows keeps first-viewport scroll length reasonable on mobile).
- No edit/delete action buttons on these rows (unlike the full `/produtos` list) — each row is a plain `<Link>` to `/produtos/[id]/editar`, keeping the dashboard read-summary-only in spirit; full management stays on the dedicated Produtos page.

### Sidebar (desktop, `md:` and up)
- `<aside className="hidden w-56 shrink-0 flex-col gap-6 border-r border-[#E7F2FD] bg-white p-4 md:flex">`, always in the DOM, hidden below `md`.
- Nav links (`Dashboard`/`Produtos`/`Configurações`) stacked vertically, `gap-3`, each `min-h-11` for touch/click-target consistency even though desktop pointer precision doesn't strictly require it (keeps one shared style across breakpoints).
- Active link: `font-medium text-[#0D21A1]` + a `border-l-2 border-[#0D21A1] pl-3 -ml-3` indicator (accent, per Color contract). Inactive: `text-[#6B6B6B]`. Active detection via `usePathname().startsWith(item.href)`.
- "Sair da conta" pinned to the bottom via `mt-auto`, visually separated by a `border-t border-[#E7F2FD] pt-4` divider (per D-07 "separado visualmente"), calling the existing `signOutAction` form — neutral styling (`text-[#6B6B6B]`), not destructive red.

### Drawer (mobile, below `md:`)
- Hamburger trigger: `<button aria-label="Abrir menu">` with `Menu` icon (`h-6 w-6`), `min-h-11 min-w-11`, positioned in a small top bar above `{children}` (the `(painel)/layout.tsx` mobile header — not present on desktop, where the sidebar is always visible instead).
- `<dialog>` native, `.showModal()` on trigger click: `className="m-0 h-dvh max-h-none w-64 max-w-none bg-white p-4 backdrop:bg-black/50"`. Slides in from the left edge (default `<dialog>` centering overridden via the sizing above; exact `left-0`/transition polish is an implementation detail, not a contract requirement — the *content and structure* below are the contract).
- Close button: `X` icon (`h-6 w-6`), `min-h-11 min-w-11`, `aria-label="Fechar menu"`, top-right of the drawer.
- Same nav links + active-state styling + "Sair da conta" footer as the desktop sidebar (single `<NavLinks>` sub-component shared between both, per `06-RESEARCH.md` Pattern 5) — copy and color rules never diverge between desktop/mobile.
- `onCancel` handler closes the dialog (Escape key support, native to `<dialog>`).

---

## Registry Safety

Not applicable — `Tool: none` (no shadcn, no component registry of any kind used in this project).

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
