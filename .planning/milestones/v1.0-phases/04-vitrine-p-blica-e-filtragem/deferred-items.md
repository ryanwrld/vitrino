# Itens Adiados — Fase 4

Itens descobertos durante a execução que estão fora do escopo da task/plan atual (não causados pelas mudanças desta execução), portanto não corrigidos automaticamente.

## Plan 04-01

- **`tests/supabase/server-cookies.test.ts` — erro pré-existente de `tsc --noEmit`** (linhas 23 e 42): `Conversion of type 'SupabaseClient<...>' to type '{ cookies: {...} }' may be a mistake`. Mesmo erro já documentado em `.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/deferred-items.md` (Plan 03-01) — confirmado de novo via `git stash` que o erro já existia antes da regeneração de `database.types.ts` desta fase (não relacionado às colunas `hide_when_sold_out`/`hide_sold_out_default`). Fora do escopo da Task 2 (arquivo não listado em `files_modified` do plano). Terceira vez que este mesmo erro é reconfirmado como pré-existente — reforça que precisa de uma correção dedicada de tipagem do mock de `SupabaseClient` nesse teste, fora do ciclo normal de fases.

## Wave 2 (Plans 04-02 + 04-05) — sweep de regressão

- **Rate-limit de `signUp` do Supabase Auth ao rodar múltiplos arquivos de teste juntos** — mesmo bloqueador herdado já documentado em `.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/deferred-items.md` (Plans 03-04/03-06) e em `STATE.md` §Blockers. Confirmado de novo: `npx vitest run tests/products/ tests/settings/ tests/storefront/` (rodando ~14 arquivos juntos) produziu `{ error: 'Request rate limit reached' }` em praticamente todo teste que faz `signUp` real — incluindo arquivos INTEIRAMENTE não tocados nesta fase (`tests/products/photo-upload.test.ts`, `tests/products/list-filter-sort.test.ts`, `tests/settings/update-slug.test.ts`, `tests/settings/slug-availability.test.ts`, da Fase 2/3), provando que é esgotamento de cota do projeto Supabase remoto de teste, não uma regressão desta fase. Todos os arquivos criados/modificados nesta fase (`tests/storefront/public-access-rls.test.ts`, `tests/storefront/list-filter-paginate.test.ts`, `tests/products/hide-when-sold-out.test.ts`, `tests/settings/hide-sold-out-default.test.ts`) já haviam sido verificados individualmente com sucesso (100% verde) logo após cada task, antes deste sweep amplo — mesma disciplina recomendada em `04-VALIDATION.md` ("rodar o arquivo isolado... em vez da suíte completa para validação rápida"). Não corrigido — fora de escopo desta fase (mudança de infraestrutura de teste, mesma conclusão já registrada 3 vezes na Fase 3). Nota adicional: re-rodar os arquivos novos desta fase isoladamente logo APÓS o sweep amplo também bateu no rate limit (cooldown ainda não decorrido) — não é sinal de regressão nova, é o mesmo esgotamento de cota se propagando; os resultados verdes originais (registrados nos respectivos `04-0N-SUMMARY.md`, capturados imediatamente após escrever cada arquivo de teste, antes de qualquer sweep amplo) permanecem a evidência de verificação válida para cada task.
