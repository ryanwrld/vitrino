---
phase: 01-funda-o-conta-e-isolamento-multi-tenant
plan: 05
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - package.json
  - src/lib/phone/normalize-br.ts
  - src/lib/validation/onboarding.ts
  - src/lib/onboarding/actions.ts
  - src/app/(admin)/onboarding/page.tsx
  - tests/phone/normalize-br.test.ts
  - tests/onboarding/store-settings.test.ts
  - tests/onboarding/message-template.test.ts
autonomous: false
requirements: [LOJA-01, WPP-01, WPP-02]

must_haves:
  truths:
    - "Onboarding coleta nome da loja, logo, cor de destaque, frase (≤100 chars) e salva em stores/store_settings (SC-5, LOJA-01, D-04)"
    - "Número de WhatsApp é normalizado para E.164 apenas-dígitos (55DDXXXXXXXXX) no servidor; entradas malformadas são rejeitadas com mensagem clara (WPP-01)"
    - "Template de mensagem é editável, pré-preenchido, e validado para conter os placeholders {modelo}/{solado}/{tamanho}/{preço} (WPP-02)"
    - "Ao concluir o onboarding, onboarding_completed_at é setado e o Dashboard passa a ser alcançável (D-04)"
  artifacts:
    - "src/lib/phone/normalize-br.ts (normalizeWhatsAppBR)"
    - "src/lib/onboarding/actions.ts (saveOnboarding)"
    - "src/lib/validation/onboarding.ts (Zod)"
    - "src/app/(admin)/onboarding/page.tsx (wizard)"
    - "tests de phone/onboarding/template"
  key_links:
    - "onboarding page -> saveOnboarding -> normalizeWhatsAppBR (uma única vez no save)"
    - "saveOnboarding -> update store_settings.onboarding_completed_at"
    - "logo upload -> bucket store-assets/{owner_id}/"
---

<objective>
Entregar o wizard de onboarding pós-cadastro (D-04): coleta identidade da loja (nome, logo, cor, frase ≤100) e configuração de WhatsApp (número normalizado + template de mensagem), grava em `stores`/`store_settings`, seta `onboarding_completed_at` e libera o Dashboard. Inclui o gate de legitimidade de pacote para `libphonenumber-js` ([ASSUMED]) antes da instalação, e a normalização de telefone feita UMA ÚNICA VEZ no servidor.

Purpose: cobre LOJA-01, WPP-01, WPP-02. A normalização de telefone aqui é consumida pela Fase 5 (botão "Pedir agora") sem poder re-derivar — normalizar na hora errada/incompleta é a Armadilha 2. Honra D-04 (onboarding antes do dashboard) e a diretriz "percebido como rápido, poucos campos obrigatórios" (CONTEXT §Ideias Específicas).
Output: revendedor completa o onboarding e chega ao Dashboard com loja e WhatsApp configurados.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-RESEARCH.md
@.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-PATTERNS.md
@.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-CONTEXT.md
@.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING-HUMAN] Verificar legitimidade de libphonenumber-js antes de instalar</name>
  <what-built>Nenhuma alteração de código ainda — este é o gate de proveniência de pacote exigido para a dependência [ASSUMED] `libphonenumber-js`, descoberta via WebSearch (não via Context7/docs oficiais).</what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/libphonenumber-js e confirmar: publisher/mantenedor legítimo (catamphetamine), repositório fonte gitlab.com/catamphetamine/libphonenumber-js, ~19,5M downloads/semana, última versão ~1.13.8.
    2. Confirmar ausência de script `postinstall` suspeito: `npm view libphonenumber-js scripts.postinstall` deve retornar vazio.
    3. Confirmar que a versão a instalar é `1.13.8` (ou a mais recente estável verificada).
  </how-to-verify>
  <resume-signal>Digite "aprovado" para autorizar `npm install libphonenumber-js@1.13.8`, ou indique uma alternativa (ex.: normalização manual) se rejeitar a dependência.</resume-signal>
  <action>
    Gate de legitimidade de pacote (T-01-SC): `libphonenumber-js` está tagueada `[ASSUMED]` no §Auditoria de Legitimidade do RESEARCH (verdict SUS motivado por "too-new" — falso positivo por alta cadência de releases, mas exige confirmação humana por ter sido descoberta via WebSearch). Este checkpoint NÃO é auto-aprovável (`auto_advance` ignorado). Só após aprovação humana instalar `libphonenumber-js@1.13.8` e prosseguir para a Task 2.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: normalize-br.ts (normalização E.164 de WhatsApp BR) + testes</name>
  <files>package.json, src/lib/phone/normalize-br.ts, tests/phone/normalize-br.test.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-PATTERNS.md (§`src/lib/phone/normalize-br.ts` — excerpt canônico)
    - .planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-RESEARCH.md (§Exemplos de Código linhas 422-434; Armadilha 2; §Não Reinvente a Roda)
    - .planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-VALIDATION.md (linha WPP-01)
  </read_first>
  <behavior>
    - normalizeWhatsAppBR("(11) 99999-9999") retorna { e164Digits: "5511999999999" }
    - Cobrir casos malformados do PITFALLS: parênteses, traços, zero à esquerda no DDD, número sem DDI, espaços não-quebráveis colados — normalizados corretamente quando válidos
    - Entrada inválida (ex.: dígitos de menos) retorna { error: "Número de WhatsApp inválido..." }, nunca um número parcial
  </behavior>
  <action>
    Após a instalação aprovada (Task 1), criar `src/lib/phone/normalize-br.ts` exportando `normalizeWhatsAppBR(input: string)` conforme o excerpt canônico do PATTERNS.md: usa `parsePhoneNumberFromString(input, 'BR')`, retorna `{ e164Digits }` (com o `+` removido de `phone.number`) quando `phone.isValid()`, senão `{ error: 'Número de WhatsApp inválido. Confira o DDD e o número.' }`. Esta função é pura e testável unitariamente; DEVE ser chamada dentro do Server Action (Task 3), nunca só no client (Armadilha 2). Criar `tests/phone/normalize-br.test.ts` cobrindo explicitamente: parênteses+traços, zero à esquerda no DDD, número de 8 dígitos legado, entrada sem DDI, espaços não-quebráveis colados, e uma entrada inválida que deve retornar `error`.
  </action>
  <verify>
    <automated>npx vitest run tests/phone/normalize-br.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lista `libphonenumber-js`
    - `tests/phone/normalize-br.test.ts` cobre parênteses/traços, zero à esquerda, sem DDI, espaços colados e caso inválido
    - Uma entrada válida com formatação produz exatamente `5511999999999` (13 dígitos, sem `+`)
    - `npx vitest run tests/phone/normalize-br.test.ts` passa
  </acceptance_criteria>
  <done>normalizeWhatsAppBR normaliza corretamente entradas reais de revendedor e rejeita inválidas; testes cobrem os casos do PITFALLS; verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wizard de onboarding + saveOnboarding (loja + WhatsApp + template) + conclusão</name>
  <files>src/lib/validation/onboarding.ts, src/lib/onboarding/actions.ts, src/app/(admin)/onboarding/page.tsx, tests/onboarding/store-settings.test.ts, tests/onboarding/message-template.test.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-RESEARCH.md (Padrão 5; §Mapa de Responsabilidade — onboarding wizard e upload de logo; Perguntas em Aberto #1 e #2)
    - .planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-PATTERNS.md (§Validation convention; §onboarding-guard)
    - .planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-CONTEXT.md (D-04, §Ideias Específicas — percebido como rápido)
    - .planning/PROJECT.md (template padrão de mensagem "Olá! Vi sua vitrine...")
  </read_first>
  <behavior>
    - saveOnboarding com dados válidos: atualiza stores (name, accent_color, tagline, logo_url) e store_settings (whatsapp_e164 normalizado, message_template), seta onboarding_completed_at = now(), redireciona para /dashboard
    - tagline > 100 caracteres é rejeitada (Zod + check constraint do banco)
    - message_template sem os placeholders obrigatórios ({modelo},{solado},{tamanho},{preço}) é rejeitado
    - número de WhatsApp inválido é rejeitado com mensagem clara (via normalizeWhatsAppBR no servidor)
  </behavior>
  <action>
    Criar `src/lib/validation/onboarding.ts` com Zod: nome da loja obrigatório, `accent_color` (formato de cor), `tagline` ≤100 caracteres (LOJA-01), `whatsapp` (string bruta, normalização no server), `message_template` com `.refine` exigindo a presença dos quatro placeholders `{modelo}`, `{solado}`, `{tamanho}`, `{preço}` (WPP-02). Criar `src/lib/onboarding/actions.ts` ('use server') com `saveOnboarding(formData)` que: valida com Zod; chama `normalizeWhatsAppBR` no servidor (Armadilha 2 — uma única vez, no save) e retorna erro claro se `error`; faz upload do logo para o bucket `store-assets/{owner_id}/logo.*` (validar content-type no servidor — §Domínio de Segurança, upload de logo malicioso); atualiza a linha `stores` e a `store_settings` do usuário atual; seta `onboarding_completed_at = now()` (Pergunta em Aberto #1: campo explícito); e `redirect('/dashboard')`. Criar `src/app/(admin)/onboarding/page.tsx` como wizard (multi-step ou tela única — a critério, decisão de UI é discrição do Claude por CONTEXT) usando react-hook-form + Zod + sonner, com o campo de template PRÉ-PREENCHIDO com o template padrão do PROJECT.md (Pergunta em Aberto #2: pré-popular, editável) e uma prévia do número formatado antes de salvar. Poucos campos obrigatórios por tela (diretriz "percebido como rápido"). Criar `tests/onboarding/store-settings.test.ts` (salva nome/cor/frase, rejeita frase >100) e `tests/onboarding/message-template.test.ts` (aceita template com os 4 placeholders, rejeita template sem eles).
  </action>
  <verify>
    <automated>npx vitest run tests/onboarding/store-settings.test.ts tests/onboarding/message-template.test.ts</automated>
    <human-check>Rodar `next dev`, completar o onboarding com nome, logo, cor, frase e um número de WhatsApp BR real; confirmar que a prévia formatada aparece, que o template vem pré-preenchido, e que ao concluir o Dashboard passa a ser alcançável.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/lib/onboarding/actions.ts` chama `normalizeWhatsAppBR` (server-side) e seta `onboarding_completed_at`
    - Validação Zod do template exige os quatro placeholders `{modelo}`, `{solado}`, `{tamanho}`, `{preço}`
    - Frase >100 caracteres é rejeitada
    - `npx vitest run tests/onboarding/store-settings.test.ts tests/onboarding/message-template.test.ts` passa
  </acceptance_criteria>
  <done>Onboarding salva identidade da loja + WhatsApp normalizado + template validado, seta onboarding_completed_at e libera o Dashboard; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formulário → saveOnboarding | Nome/cor/frase/telefone/template revalidados no servidor (Zod + normalização) |
| upload de logo → Storage | Arquivo não confiável validado por content-type e escopado a {owner_id}/ |
| app → npm registry | Instalação de libphonenumber-js ([ASSUMED]) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Tampering | normalização de telefone client-only | high | mitigate | normalizeWhatsAppBR chamado DENTRO do Server Action, rejeitando inválidos (Armadilha 2); Fase 5 apenas lê o valor já normalizado |
| T-01-15 | Tampering | upload de logo malicioso | medium | mitigate | Validar content-type/magic-bytes no servidor; policy de Storage escopa a {owner_id}/ (Plan 02) |
| T-01-16 | Input validation | frase/template inválidos | medium | mitigate | Zod (frase ≤100, placeholders obrigatórios) + check constraint char_length no banco |
| T-01-SC | Tampering | npm install libphonenumber-js ([ASSUMED]) | high | mitigate | Checkpoint blocking-human (Task 1) verifica npmjs.com, downloads e ausência de postinstall antes de instalar — nunca auto-aprovável |
</threat_model>

## Artifacts this phase produces (Plan 05)

- **Função pura:** `normalizeWhatsAppBR` (`src/lib/phone/normalize-br.ts`)
- **Server Action:** `saveOnboarding` (`src/lib/onboarding/actions.ts`)
- **Zod schemas:** onboarding (`src/lib/validation/onboarding.ts`) — nome/cor/tagline≤100/whatsapp/template com 4 placeholders
- **Rota:** `(admin)/onboarding/page.tsx` (wizard)
- **Dependência:** `libphonenumber-js@1.13.8` (aprovada via gate)
- **Tests:** `tests/phone/normalize-br.test.ts`, `tests/onboarding/store-settings.test.ts`, `tests/onboarding/message-template.test.ts`

## Verificações Manuais de Fim de Fase (documentadas — human_verify_mode=end-of-phase)

Executar após todos os planos, antes de `/gsd:verify-work`. Itens `manual-only` do VALIDATION.md que não são automatizáveis de forma confiável:

| # | Requisito | Passos | Resultado esperado |
|---|-----------|--------|--------------------|
| M-1 | AUTH-02 | Fazer login em /login; aguardar; dar refresh (F5) na página do painel | Permanece autenticado após o reload (sessão em cookie httpOnly persiste) |
| M-2 | AUTH-04 | Deixar a sessão ociosa além da expiração do token em uso normal; depois simular perda de conectividade e tentar salvar algo | Renovação silenciosa em uso normal (sem aviso); aviso "Sua sessão expirou..." aparece SOMENTE quando a renovação falha de verdade (D-03) |
| M-3 | AUTH-05 | Fluxo completo de reset por email com o template TokenHash configurado no painel | Link do email leva a /redefinir-senha e a nova senha funciona |
| M-4 | LOJA-01/WPP-01 | Completar onboarding com número BR real; conferir a prévia formatada | Número exibido/salvo como 55DDXXXXXXXXX; Dashboard liberado só após concluir |

<verification>
- `npx vitest run` (suíte completa) verde, incluindo phone/onboarding/RLS/auth/middleware
- `npx tsc --noEmit` limpo
- Checklist manual M-1..M-4 confirmado
</verification>

<success_criteria>
Onboarding coleta identidade da loja e WhatsApp (número normalizado no servidor + template validado), seta onboarding_completed_at e libera o Dashboard; libphonenumber-js aprovada via gate de legitimidade. (Cobre LOJA-01, WPP-01, WPP-02; SC-5.)
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-05-SUMMARY.md` when done
</output>
