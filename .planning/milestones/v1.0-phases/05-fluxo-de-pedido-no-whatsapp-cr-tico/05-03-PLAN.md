---
phase: 05-fluxo-de-pedido-no-whatsapp-cr-tico
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/products/public-list.ts
  - src/lib/products/public-detail.ts
  - src/app/loja/[slug]/product-card.tsx
  - src/app/loja/[slug]/product-grid.tsx
  - src/app/loja/[slug]/load-more-button.tsx
  - tests/storefront/product-detail.test.ts
autonomous: true
requirements: [PED-01, PED-02]
user_setup: []

must_haves:
  truths:
    - "queryPublicProductDetail retorna o produto publicado + visível com o mapa completo de tamanhos (size, available) e a galeria completa de fotos ordenada"
    - "queryPublicProductDetail retorna null para produto inexistente, não publicado, OU oculto pela regra de esgotado (reusa isVisible verbatim)"
    - "Cada card da vitrine vira um Link para /loja/[slug]/[produto] usando o id do produto (D-01: página de detalhe dedicada, não modal/accordion inline no grid)"
    - "product-grid.tsx e load-more-button.tsx encaminham o slug ao ProductCard sem quebrar o build"
  artifacts:
    - src/lib/products/public-detail.ts
    - tests/storefront/product-detail.test.ts
  key_links:
    - "public-detail.ts importa isVisible de public-list.ts (exportado aqui), nunca re-deriva a regra de esgotado (Pitfall 8 — bypass por link direto)"
    - "ProductCard recebe slug e monta href=/loja/${slug}/${product.id}"
  prohibitions:
    - statement: "Nunca criar uma segunda função de detalhe público que re-derive a predicate de esgotado inline — reusar isVisible()"
      status: resolved
      verification: "public-detail.ts importa isVisible de public-list.ts; tests/storefront/product-detail.test.ts prova null para produto oculto por esgotado"
    - statement: "public-detail.ts nunca importa ou modifica list.ts (admin, owner-scoped) — funções paralelas"
      status: resolved
      verification: "revisão de imports de public-detail.ts"
---

<objective>
Fatia de dados + navegação que abre caminho para o detalhe do produto: exporta `isVisible()` de `public-list.ts` e cria `queryPublicProductDetail()` (variante single-row do padrão duas-queries-mais-join já estabelecido), retornando o mapa completo de tamanhos (para as pílulas, PED-02) e a galeria completa de fotos. Reusa `isVisible()` verbatim para que um produto oculto pela regra de esgotado (Fase 4) resolva como "não encontrado" também no link direto (Pitfall 8 — sem bypass). Transforma o card do grid num `<Link>` para `/loja/[slug]/[produto]`, encaminhando o `slug` pelos componentes que instanciam o card.

Purpose: Entregar a camada de leitura e a navegação que a página de detalhe (05-04) consome, com o guard de visibilidade correto por design.
Output: query pública de detalhe testada + card navegável.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-RESEARCH.md
@.planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Criados/modificados neste plano:
- `src/lib/products/public-detail.ts` — `queryPublicProductDetail()` + o tipo do detalhe retornado
- `src/lib/products/public-list.ts` — `isVisible()` passa a ser exportado
- `src/app/loja/[slug]/product-card.tsx` — envolvido em `<Link>`, prop `slug` adicionada a `ProductCard`/`PublicProductCardData` (ou href pré-montado)
- `src/app/loja/[slug]/product-grid.tsx`, `src/app/loja/[slug]/load-more-button.tsx` — encaminham `slug`
- `tests/storefront/product-detail.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Exportar isVisible + queryPublicProductDetail + teste de integração</name>
  <files>src/lib/products/public-list.ts, src/lib/products/public-detail.ts, tests/storefront/product-detail.test.ts</files>
  <read_first>
    - src/lib/products/public-list.ts (isVisible L70-73 a exportar; padrão duas-queries-mais-join L119-158; tipos PublicProduct)
    - src/app/(admin)/produtos/[id]/editar/page.tsx (L44-65: resolve-by-id + fetch de sizes/photos — adaptar, mas com notFound/visibility em vez de redirect)
    - src/lib/products/constants.ts (BRANDS/SOLES/CATEGORIES/FULFILLMENTS para labels)
    - tests/storefront/sold-out-visibility.test.ts (shape de seed + assert L14-37 — espelhar as 5 combinações)
    - tests/setup/supabase-test.ts (seedAuthenticatedAccount)
    - .planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-PATTERNS.md (seções public-detail.ts e product-detail.test.ts com o shape exato)
  </read_first>
  <action>
    1. Em `src/lib/products/public-list.ts`: adicionar `export` antes de `function isVisible` (L70). Nada mais muda nessa função.
    2. Criar `src/lib/products/public-detail.ts` com doc-header no estilo de public-list.ts (função PARALELA à admin, NUNCA importar/modificar list.ts). Exportar `queryPublicProductDetail(supabase: SupabaseClient<Database>, storeId: string, productId: string, storeHideSoldOutDefault: boolean)` retornando `null` ou um objeto de detalhe. Passos: buscar o produto por `id` + `store_id` + `status='published'` com `.single()` (incluir hide_when_sold_out); se ausente, retornar null. Buscar TODAS as linhas de `product_sizes` (`size, available`, ordenado por size asc) — o mapa completo por tamanho, não um booleano agregado. Buscar TODAS as `product_photos` (`id, storage_path`, ordenado por position asc) — galeria completa, não só a capa. Derivar `disponivel` = existe algum size com available=true e aplicar `isVisible(product.hide_when_sold_out, disponivel, storeHideSoldOutDefault)`: se false, retornar null (produto oculto por esgotado resolve como não encontrado — Pitfall 8). Retornar o shape com campos do produto (id, name, brand, brand_other, line, sole, price), array de tamanhos {size, available}, array de fotos {id, storage_path}. Definir e exportar o tipo do retorno.
    3. Criar `tests/storefront/product-detail.test.ts` no shape de sold-out-visibility.test.ts (seed via seedAuthenticatedAccount, timeout 30000ms). Casos: produto publicado+visível retorna objeto com arrays de tamanhos e fotos; produto inexistente → null; produto rascunho (status != published) → null; produto publicado porém oculto pela regra de esgotado (hide_when_sold_out=true, todos os tamanhos esgotados) → null.
  </action>
  <verify>
    <automated>npx vitest run tests/storefront/product-detail.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `isVisible` exportado de public-list.ts (import funciona de public-detail.ts)
    - queryPublicProductDetail retorna arrays completos de tamanhos e fotos para produto visível
    - Retorna null para inexistente, rascunho e oculto-por-esgotado (reusando isVisible, sem re-derivar)
    - public-detail.ts NÃO importa list.ts
    - `npx vitest run tests/storefront/product-detail.test.ts` verde; `npx tsc --noEmit` limpo
  </acceptance_criteria>
  <done>Query de detalhe público entregue com guard de visibilidade correto e provada por integração real.</done>
</task>

<task type="auto">
  <name>Task 2: Card do grid vira Link para a rota de detalhe (encaminhar slug)</name>
  <files>src/app/loja/[slug]/product-card.tsx, src/app/loja/[slug]/product-grid.tsx, src/app/loja/[slug]/load-more-button.tsx</files>
  <read_first>
    - src/app/loja/[slug]/product-card.tsx (arquivo inteiro — L28 é o wrap target; assinatura atual só recebe {product})
    - src/app/loja/[slug]/product-grid.tsx (call site do ProductCard)
    - src/app/loja/[slug]/load-more-button.tsx (já tem slug como prop; call site do ProductCard)
    - src/app/(admin)/produtos/[id]/editar/page.tsx (convenção de import de next/link L1, L102-107)
  </read_first>
  <action>
    1. product-card.tsx: importar `Link` de `next/link`. Adicionar `slug: string` à assinatura de `ProductCard` (ex. `{ product, slug }`) — não poluir `PublicProductCardData` com slug; passar como prop separada. Trocar o `<div className="flex flex-col gap-2">` externo (L28) por `<Link href={`/loja/${slug}/${product.id}`} className="flex flex-col gap-2">`, mantendo tudo dentro inalterado (imagem, nome, preço, badge de disponibilidade). O `[produto]` é o `id` (UUID) do produto — decisão A3 do research (URL compartilhável satisfeita sem coluna slug nova).
    2. product-grid.tsx: adicionar `slug: string` ao tipo de props e passar `slug={slug}` a cada `<ProductCard>`. Atualizar o call site em page.tsx (`<ProductGrid products={...} />`) para passar `slug={slug}` (slug já está em escopo via params).
    3. load-more-button.tsx: passar `slug={slug}` no `<ProductCard>` (slug já é destructurado na assinatura — mudança de uma linha).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ProductCard renderiza um `<Link href="/loja/${slug}/${product.id}">` envolvendo o conteúdo do card
    - product-grid.tsx e load-more-button.tsx passam slug ao ProductCard; page.tsx passa slug ao ProductGrid
    - `npx tsc --noEmit` e `npm run build` passam (nenhum call site do ProductCard quebrado)
  </acceptance_criteria>
  <done>Card navegável para a rota de detalhe; build íntegro com slug encaminhado por toda a cadeia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente anônimo → Postgres (RLS) | leitura pública de products/product_sizes/product_photos já protegida pelas policies anon `for select` da Fase 4 (status='published') |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information Disclosure | Bypass da regra de esgotado por link direto ao detalhe (produto oculto acessível via URL) | medium | mitigate | queryPublicProductDetail reusa isVisible() e retorna null → página chama notFound(); teste cobre o caso oculto |
| T-05-08 | Elevation of Privilege | Mistura acidental de lógica owner-scoped (list.ts) na função pública | low | mitigate | public-detail.ts nunca importa list.ts; doc-header explícito de função paralela |
| T-05-SC | Tampering | Instalação de pacote | n/a | accept | Nenhum pacote novo nesta fase — gate não se aplica |
</threat_model>

<verification>
- `npx vitest run tests/storefront/product-detail.test.ts` verde
- `npx tsc --noEmit` e `npm run build` limpos
</verification>

<success_criteria>
A camada de leitura de detalhe respeita publicação E a regra de esgotado (sem bypass por link), expõe tamanhos e fotos completos, e o card do grid navega para a rota de detalhe do produto.
</success_criteria>

<output>
Create `.planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-03-SUMMARY.md` when done
</output>
