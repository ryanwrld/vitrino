---
phase: 05-fluxo-de-pedido-no-whatsapp-cr-tico
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/whatsapp/order-message.ts
  - src/lib/whatsapp/order-guard.ts
  - src/lib/storage/product-image-url.ts
  - tests/products/order-message.test.ts
  - tests/products/order-button-guard.test.ts
autonomous: true
requirements: [PED-03]
user_setup: []

must_haves:
  truths:
    - "interpolateMessageTemplate substitui {modelo}, {solado}, {tamanho}, {preço} (com ç literal) no template da loja"
    - "buildOrderMessage compõe a string completa e anexa a linha 'Foto: <url>' quando há foto de capa, e não anexa quando fotoUrl é null"
    - "buildWhatsAppUrl aplica encodeURIComponent EXATAMENTE UMA VEZ sobre a mensagem inteira já composta (template + linha de foto), gerando https://wa.me/<digitos>?text=<encoded>"
    - "Caracteres acentuados (ã, ç, é) e template multi-linha sobrevivem à codificação sem codificação dupla nem saída parcial"
    - "decideOrderAction(selectedSize) retorna {shouldNavigate:false, shouldShake:true} sem tamanho e {shouldNavigate:true, shouldShake:false} com tamanho"
    - "getProductImagePublicUrl(supabase, path) retorna a public URL do bucket product-images, ou null quando path é null"
  artifacts:
    - src/lib/whatsapp/order-message.ts
    - src/lib/whatsapp/order-guard.ts
    - src/lib/storage/product-image-url.ts
    - tests/products/order-message.test.ts
    - tests/products/order-button-guard.test.ts
  key_links:
    - "{preço} usa formatBRLPriceInput (só o número '199,90'), nunca formatBRLPrice (que prefixa 'R$'), porque o template padrão já tem 'Preço: R$ {preço}' fixo"
    - "encodeURIComponent chamado uma única vez em buildWhatsAppUrl, jamais sobre sub-pedaços"
  prohibitions:
    - statement: "Nunca re-normalizar o número de WhatsApp em tempo de clique — whatsapp_e164 já vem normalizado do onboarding (Fase 1); Fase 5 só lê"
      status: resolved
      verification: "order-message.ts não importa libphonenumber-js nem normalizeWhatsAppBR"
    - statement: "Nunca codificar o template e a URL da foto separadamente (evita codificação dupla/parcial)"
      status: resolved
      verification: "tests/products/order-message.test.ts assertando ausência de %2520 e presença correta de acentos codificados uma vez"
---

<objective>
Extrai a lógica pura e testável do fluxo de pedido — a única fatia de PED-03 com cobertura automatizável forte (funções puras, sem DOM). `order-message.ts` monta a mensagem do WhatsApp (interpolação dos 4 placeholders travados + linha de foto D-06 + `encodeURIComponent` uma única vez sobre a string completa). `order-guard.ts` isola a decisão do botão "Pedir agora" (`selectedSize` → navegar ou sacudir) para dar ao PED-04 uma asserção de lógica testável sem browser. `product-image-url.ts` consolida o one-liner de URL pública do Storage hoje duplicado em dois arquivos, satisfazendo a discrição de CONTEXT.md ("reaproveitar helper existente... se houver").

Purpose: Travar cedo (Wave 1) a correção de encoding — a armadilha #6 do catálogo de bugs do PROJECT.md — com feedback em milissegundos.
Output: três módulos utilitários puros + dois arquivos de teste Vitest verdes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-RESEARCH.md
@.planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Criados neste plano:
- `src/lib/whatsapp/order-message.ts` — `interpolateMessageTemplate()`, `buildOrderMessage()`, `buildWhatsAppUrl()`
- `src/lib/whatsapp/order-guard.ts` — `decideOrderAction()` (retorna `{ shouldNavigate, shouldShake }`)
- `src/lib/storage/product-image-url.ts` — `getProductImagePublicUrl()`
- `tests/products/order-message.test.ts`, `tests/products/order-button-guard.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulos puros order-message.ts, order-guard.ts e product-image-url.ts</name>
  <files>src/lib/whatsapp/order-message.ts, src/lib/whatsapp/order-guard.ts, src/lib/storage/product-image-url.ts</files>
  <behavior>
    - interpolateMessageTemplate(template, {modelo, solado, tamanho, preco}) → substitui as 4 chaves (incluindo {preço} com ç literal) via replaceAll
    - buildOrderMessage(template, {...vars, fotoUrl}) → interpola e, se fotoUrl não-null, anexa "\n\nFoto: <url>"; se null, retorna só a base
    - buildWhatsAppUrl(phoneE164Digits, message) → `https://wa.me/${phoneE164Digits}?text=${encodeURIComponent(message)}` (uma única codificação)
    - decideOrderAction(selectedSize: number | null) → sem tamanho: {shouldNavigate:false, shouldShake:true}; com tamanho: {shouldNavigate:true, shouldShake:false}
    - getProductImagePublicUrl(supabase, storagePath: string | null) → public URL ou null quando storagePath é null
  </behavior>
  <read_first>
    - src/lib/currency/brl.ts (formatBRLPriceInput L60-62 — usar ESTE para {preço}, não formatBRLPrice)
    - src/lib/validation/onboarding.ts (DEFAULT_MESSAGE_TEMPLATE, REQUIRED_TEMPLATE_PLACEHOLDERS L10-25 — nomes exatos das chaves, incluindo {preço} com ç; importar read-only, nunca redefinir)
    - src/lib/phone/normalize-br.ts (confirmar convenção de retorno; e a nota de que whatsapp_e164 já é normalizado no onboarding — NÃO re-derivar aqui)
    - src/app/loja/[slug]/page.tsx (L83-88: one-liner getPublicUrl a extrair)
    - src/lib/products/public-actions.ts (L58-63: segunda cópia byte-a-byte do mesmo one-liner)
    - .planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-RESEARCH.md (L306-334: implementação alvo das 3 funções de mensagem)
  </read_first>
  <action>
    Criar três módulos (sem blocos de código aqui — seguir as assinaturas do research):

    src/lib/whatsapp/order-message.ts (primeiro arquivo do novo diretório src/lib/whatsapp/): exportar `interpolateMessageTemplate(template, vars)` que faz `replaceAll` das 4 chaves `{modelo}`/`{solado}`/`{tamanho}`/`{preço}` (o `ç` é literal e precisa casar com REQUIRED_TEMPLATE_PLACEHOLDERS); `buildOrderMessage(template, vars)` que interpola e anexa `\n\nFoto: ${vars.fotoUrl}` apenas quando `fotoUrl` não é null; `buildWhatsAppUrl(phoneE164Digits, message)` que retorna `https://wa.me/${phoneE164Digits}?text=${encodeURIComponent(message)}` — encodeURIComponent chamado UMA vez sobre a string completa, nunca sobre sub-pedaços. Doc-comment no estilo de brl.ts explicando: {preço} recebe só o número via formatBRLPriceInput (o template já tem "Preço: R$ {preço}" fixo — usar formatBRLPrice duplicaria o "R$", Pitfall 7); e que o telefone NÃO é re-normalizado aqui.

    src/lib/whatsapp/order-guard.ts: exportar `decideOrderAction(selectedSize: number | null): { shouldNavigate: boolean; shouldShake: boolean }` — a decisão pura do CTA (PED-04) isolada do DOM. Sem tamanho → não navega, sacode; com tamanho → navega, não sacode.

    src/lib/storage/product-image-url.ts: exportar `getProductImagePublicUrl(supabase, storagePath: string | null): string | null` — wrapper de `supabase.storage.from("product-images").getPublicUrl(path).data.publicUrl`, retornando null quando storagePath é null (casa o ternário `? ... : null` dos dois call sites existentes). Tipar `supabase` como `SupabaseClient<Database>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/whatsapp/order-message.ts exporta interpolateMessageTemplate, buildOrderMessage, buildWhatsAppUrl
    - {preço} é preenchido via formatBRLPriceInput (import de brl.ts), nunca formatBRLPrice
    - order-message.ts NÃO importa libphonenumber-js nem normalizeWhatsAppBR
    - src/lib/whatsapp/order-guard.ts exporta decideOrderAction com o contrato {shouldNavigate, shouldShake}
    - src/lib/storage/product-image-url.ts exporta getProductImagePublicUrl retornando null para path null
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>Três módulos puros criados com as assinaturas do research; encoding único e sem re-normalização de telefone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes unitários de mensagem e guarda de pedido</name>
  <files>tests/products/order-message.test.ts, tests/products/order-button-guard.test.ts</files>
  <behavior>
    - order-message.test.ts: interpolação preenche as 4 chaves do DEFAULT_MESSAGE_TEMPLATE; nome com acento (ã, ç, é) sobrevive; encodeURIComponent aplicado uma vez (sem %2520); linha "Foto: <url>" presente com fotoUrl e ausente sem; {preço} não duplica "R$"
    - order-button-guard.test.ts: decideOrderAction(null) → {shouldNavigate:false, shouldShake:true}; decideOrderAction(42) → {shouldNavigate:true, shouldShake:false}
  </behavior>
  <read_first>
    - tests/slug/slugify.test.ts (shape puro describe/it/expect sem DB nem mocking — espelhar)
    - src/lib/validation/onboarding.ts (DEFAULT_MESSAGE_TEMPLATE para montar o caso base)
    - src/lib/whatsapp/order-message.ts (funções sob teste — criadas na Task 1)
    - src/lib/whatsapp/order-guard.ts (função sob teste)
  </read_first>
  <action>
    Criar dois testes puros (ambiente node, sem DB, sem mocking), no shape de slugify.test.ts.

    tests/products/order-message.test.ts: importar de `@/lib/whatsapp/order-message`. Casos: (a) interpolar DEFAULT_MESSAGE_TEMPLATE com modelo acentuado (ex. "Nike Mercurial São Paulo"), solado, tamanho, preco="199,90" e assertar que as 4 chaves foram substituídas e o "R$" não aparece duplicado; (b) buildOrderMessage com fotoUrl não-null contém "Foto: https://..." e com fotoUrl null NÃO contém "Foto:"; (c) buildWhatsAppUrl começa com "https://wa.me/" e o text está codificado uma única vez — assertar que o resultado NÃO contém a sequência de dupla-codificação de "%" (verificar ausência de "%25" derivado de um espaço/acento re-codificado) e que `decodeURIComponent` do query param reconstrói a mensagem original com acentos intactos.

    tests/products/order-button-guard.test.ts: importar `decideOrderAction` de `@/lib/whatsapp/order-guard`. Assertar os dois ramos (null e um número).
  </action>
  <verify>
    <automated>npx vitest run tests/products/order-message.test.ts tests/products/order-button-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - order-message.test.ts cobre: substituição das 4 chaves, acentos, codificação única (round-trip via decodeURIComponent), linha de foto presente/ausente, ausência de "R$" duplicado
    - order-button-guard.test.ts cobre os dois ramos de decideOrderAction
    - `npx vitest run` dos dois arquivos: verde em milissegundos
  </acceptance_criteria>
  <done>PED-03 (fatia automatizável) e a lógica do PED-04 provados por teste puro rápido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados do produto → string da mensagem → URL wa.me | conteúdo controlado pelo revendedor (nome/solado) e pelo template entra na URL; risco é encoding, não injeção de HTML (sem dangerouslySetInnerHTML no projeto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | Codificação dupla/parcial da mensagem (bug #6 do PROJECT.md) | medium | mitigate | encodeURIComponent uma única vez sobre a string completa (buildWhatsAppUrl); teste de round-trip com acentos |
| T-05-06 | Tampering | XSS via nome/descrição do produto renderizado ou embutido no link | low | accept | Escape padrão de JSX do React é suficiente; nenhum dangerouslySetInnerHTML introduzido; a mensagem é texto puro num href de anchor |
| T-05-SC | Tampering | Instalação de pacote | n/a | accept | Nenhum pacote novo nesta fase — gate não se aplica |
</threat_model>

<verification>
- `npx vitest run tests/products/order-message.test.ts tests/products/order-button-guard.test.ts` verde
- `npx tsc --noEmit` limpo
</verification>

<success_criteria>
A mensagem de pedido é montada e codificada corretamente (uma vez, com acentos preservados, foto anexada como link), e a decisão do botão é pura e testada — tudo com feedback em milissegundos, sem DOM.
</success_criteria>

<output>
Create `.planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-02-SUMMARY.md` when done
</output>
