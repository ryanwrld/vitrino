# Supabase — Dashboard -> Project Settings -> API
# (ou saída de `supabase start` para o ambiente local)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Opcional, SÓ para rodar a suíte de testes local (tests/setup/supabase-test.ts).
# NUNCA usar NEXT_PUBLIC_ nem expor no client/produção — bypassa TODA policy RLS.
# Sem esta chave, os testes caem de volta no signUp público (sujeito a rate limit).
SUPABASE_SERVICE_ROLE_KEY=
